import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.cc2b3d55.js";
const __pageData = JSON.parse('{"title":"权限模式","description":"","frontmatter":{"title":"权限模式","lang":"zh-CN"},"headers":[],"relativePath":"guide/development/permission-mode.md"}');
const _sfc_main = { name: "guide/development/permission-mode.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="权限模式" tabindex="-1">权限模式 <a class="header-anchor" href="#权限模式" aria-label="Permalink to &quot;权限模式&quot;">​</a></h1><p>Grow Admin 通过 <strong><code>permissionMode</code></strong> 决定登录后动态菜单与路由从哪里来、如何拼装。角色信息只挂在 <code>UserInfo.roles</code> 上（用 <code>role.value</code> 做白名单匹配），<strong>没有</strong>单独的「ROLE 权限模式」枚举项。</p><p>本文说明三种模式的设计动机、配置方式、开发步骤与常见踩坑。更偏「路由结构本身」的内容见 <a href="/guide/architecture/routing-and-menu">路由与菜单</a>。</p><h2 id="设计理念-为什么要三种模式" tabindex="-1">设计理念：为什么要三种模式？ <a class="header-anchor" href="#设计理念-为什么要三种模式" aria-label="Permalink to &quot;设计理念：为什么要三种模式？&quot;">​</a></h2><table><thead><tr><th>模式</th><th>适合谁</th><th>核心假设</th></tr></thead><tbody><tr><td><strong>BACK</strong></td><td>菜单完全由后台管理系统下发</td><td>「有哪些页面」以后端为准，前端只提供组件映射</td></tr><tr><td><strong>FRONT</strong></td><td>前端写死路由表，按登录角色裁剪</td><td>无菜单接口或接口不可用，也能开发演示</td></tr><tr><td><strong>MIXTURE</strong></td><td>基础能力前端兜底，运营菜单后端扩展</td><td>同名冲突时以后端为准，避免双份数据互相覆盖错乱</td></tr></tbody></table><p><strong>为什么去掉旧的 ROLE / ROUTE_MAPPING？</strong></p><ul><li>角色过滤是横切能力，不是「菜单来源」；应挂在 FRONT / MIXTURE 的前端树上，而不是再占一个模式名。</li><li><code>ROUTE_MAPPING</code> 语义模糊，已重命名为更直观的 <strong>FRONT</strong>（前端路由表）。</li></ul><p><strong>为什么 MIXTURE 同名整条用后端？</strong></p><p>合集时若字段级 merge，极易出现「title 来自前端、path 来自后端」的半成品节点。约定：<strong>同名节点整条以后端为准</strong>，<code>children</code> 再递归合集——行为可预期，调试成本更低。</p><h2 id="三种模式对照" tabindex="-1">三种模式对照 <a class="header-anchor" href="#三种模式对照" aria-label="Permalink to &quot;三种模式对照&quot;">​</a></h2><table><thead><tr><th>枚举</th><th>值</th><th>路由注册数据</th><th>authStore 写入</th><th>侧栏最终展示</th></tr></thead><tbody><tr><td><code>PermissionModeEnum.BACK</code></td><td><code>BACK</code></td><td>仅 <code>GET /api/menu/list</code></td><td><code>backMenuList</code></td><td><code>backMenuList</code>（按 <code>sort</code>）</td></tr><tr><td><code>PermissionModeEnum.FRONT</code></td><td><code>FRONT</code></td><td><code>toFeatRouteConfigs()</code> → 角色过滤</td><td><code>frontMenuList</code></td><td><code>frontMenuList</code>（按 <code>sort</code>）</td></tr><tr><td><code>PermissionModeEnum.MIXTURE</code></td><td><code>MIXTURE</code></td><td>前端（先过滤）∪ 后端合集</td><td>两边都写；路由用合集</td><td><code>mergeTreesByName(front, back)</code></td></tr></tbody></table><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">BACK     → getMenuList() ──────────────────────────→ backMenuList + 注册路由</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">FRONT    → toFeatRouteConfigs → filterByRoles ─────→ frontMenuList + 注册路由</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">MIXTURE  → front(过滤) ∪ back → mergeTreesByName ─→ 合集注册路由</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">           （同时仍分别写入 frontMenuList / backMenuList）</span></span></code></pre></div><p>核心实现：<code>cornerstone-apps-home/src/routes/registerDynamicRoutes.ts</code>。</p><h2 id="如何配置" tabindex="-1">如何配置 <a class="header-anchor" href="#如何配置" aria-label="Permalink to &quot;如何配置&quot;">​</a></h2><h3 id="_1-切换模式-必做" tabindex="-1">1. 切换模式（必做） <a class="header-anchor" href="#_1-切换模式-必做" aria-label="Permalink to &quot;1. 切换模式（必做）&quot;">​</a></h3><p>宿主静态配置：</p><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#676E95", "font-style": "italic" })}">// sample/src/projectSetting.ts</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">import</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">PermissionModeEnum</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">from</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">@grow-admin-rock/constants</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">export</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#C792EA" })}">const</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> projectSetting</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#FFCB6B" })}">ProjectSetting</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">=</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">permissionMode</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> PermissionModeEnum</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">MIXTURE</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#676E95", "font-style": "italic" })}">// BACK | FRONT | MIXTURE</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">  </span><span style="${ssrRenderStyle({ "color": "#676E95", "font-style": "italic" })}">// ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span></span></code></pre></div><h3 id="_2-启动同步-已内置" tabindex="-1">2. 启动同步（已内置） <a class="header-anchor" href="#_2-启动同步-已内置" aria-label="Permalink to &quot;2. 启动同步（已内置）&quot;">​</a></h3><p><code>sample/src/initAppConfig.ts</code> 每次启动都会把 <code>permissionMode</code> 从 <code>projectSetting</code> 同步进 <code>useAppConfig()</code>：</p><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">appConfig</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">$patch</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">(</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">lockTime</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> mappedConfig</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">lockTime</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">useLockPage</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> mappedConfig</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">useLockPage</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">permissionMode</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> configuredMode</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#676E95", "font-style": "italic" })}">// 以配置文件为准</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">)</span></span></code></pre></div><p>因此：<strong>改 <code>projectSetting.ts</code> 即可切换模式</strong>，不必依赖设置抽屉持久化字段。</p><h3 id="_3-模式变更与本地缓存" tabindex="-1">3. 模式变更与本地缓存 <a class="header-anchor" href="#_3-模式变更与本地缓存" aria-label="Permalink to &quot;3. 模式变更与本地缓存&quot;">​</a></h3><p>框架用 <code>LAST_PERMISSION_MODE</code> 记录上次成功应用的模式。若与当前配置不一致：</p><ol><li>清空菜单 / 标签相关状态（<code>clearPermissionRelatedCaches</code>）</li><li>动态路由层调用 <code>resetRouter()</code></li><li>守卫若发现目标 URL 在新模式下不可达，则 <code>next({ name: &#39;Home&#39; })</code>，避免旧书签白屏</li></ol><table><thead><tr><th>存储</th><th>Key（带 <code>createStorageName</code> 前缀）</th><th>说明</th></tr></thead><tbody><tr><td>localStorage</td><td><code>__LAST_PERMISSION_MODE</code></td><td>上次生效模式</td></tr><tr><td>sessionStorage</td><td><code>__TAB</code></td><td>标签页；模式变更时清除</td></tr><tr><td>localStorage</td><td><code>__APP_CONFIG</code></td><td>应用配置；<code>permissionMode</code> 仍以 projectSetting 覆盖</td></tr></tbody></table><p>实现：<code>rock-state/src/modules/permissionModeCache.ts</code>、<code>registerDynamicRoutes</code>、<code>guard.ts</code>。</p><h3 id="_4-运行时读取模式" tabindex="-1">4. 运行时读取模式 <a class="header-anchor" href="#_4-运行时读取模式" aria-label="Permalink to &quot;4. 运行时读取模式&quot;">​</a></h3><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">import</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">Lib</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">as</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">routeLib</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">from</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">@grow-admin-rock/middleware-router</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">import</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">resolveByKeyOrThrow</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">from</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">@grow-admin-rock/ioc</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#C792EA" })}">const</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> menuState </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">=</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">resolveByKeyOrThrow</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">(routeLib</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">types</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">MenuState)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">menuState</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">isBackMode</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">()</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">menuState</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">isFrontMode</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">()</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">menuState</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">isMixtureMode</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">()</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">menuState</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">getPermissionMode</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">()</span></span></code></pre></div><p><code>MenuState</code> 内部读的是 <code>useAppConfig().permissionMode</code>。</p><h2 id="mixture-合集规则" tabindex="-1">MIXTURE 合集规则 <a class="header-anchor" href="#mixture-合集规则" aria-label="Permalink to &quot;MIXTURE 合集规则&quot;">​</a></h2><p><code>mergeTreesByName(frontList, backList)</code>（<code>rock-state/src/modules/mergeTreesByName.ts</code>）：</p><ol><li><strong>先铺前端</strong>，再叠后端</li><li>后端出现<strong>新 name</strong>：追加</li><li>后端与前端<strong>同名</strong>：整条节点采用后端数据；两边的 <code>children</code> <strong>再按 name 递归合集</strong></li><li>结果按 <strong><code>sort</code> 升序</strong>（缺省 <code>0</code>）</li></ol><p>侧栏在 MIXTURE 下也会再次 <code>mergeTreesByName(frontMenuList, backMenuList)</code>，与注册路由用的合集规则一致。</p><h3 id="演示约定-sample" tabindex="-1">演示约定（sample） <a class="header-anchor" href="#演示约定-sample" aria-label="Permalink to &quot;演示约定（sample）&quot;">​</a></h3><table><thead><tr><th>name</th><th>来源</th><th>用途</th></tr></thead><tbody><tr><td><code>MixtureDemoCatalog</code></td><td>两端可共存</td><td>目录「权限演示」</td></tr><tr><td><code>MixtureFrontDemo</code></td><td>仅前端 <code>FEAT_FRONT_ONLY_*</code></td><td>验证「前端独有」进合集</td></tr><tr><td><code>MixtureBackDemo</code></td><td>仅后端 workspace + Mock</td><td>验证「后端独有」进合集</td></tr></tbody></table><p>建议顶层 <code>sort</code>：Dashboard <code>10</code>、功能示例 <code>20</code>、权限演示 <code>30</code>、外部页面 <code>40</code>。</p><div class="tip custom-block"><p class="custom-block-title">前端独有项不要进 Mock</p><p><code>FEAT_FRONT_ONLY_MENU_LIST</code> / <code>FEAT_FRONT_ONLY_STRUCTURES</code> <strong>不要</strong>并进会返回给 <code>/api/menu/list</code> 的列表，否则 BACK / MIXTURE 后端侧也会带上「本应只存在于前端」的菜单。</p></div><h2 id="front-mixture-角色过滤怎么配" tabindex="-1">FRONT / MIXTURE：角色过滤怎么配 <a class="header-anchor" href="#front-mixture-角色过滤怎么配" aria-label="Permalink to &quot;FRONT / MIXTURE：角色过滤怎么配&quot;">​</a></h2><h3 id="角色从哪来" tabindex="-1">角色从哪来 <a class="header-anchor" href="#角色从哪来" aria-label="Permalink to &quot;角色从哪来&quot;">​</a></h3><p>登录人角色来自 <code>UserInfo.roles</code>：</p><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#FFCB6B" })}">roles</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> [</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">name</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">Super Admin</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">value</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">super</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">]</span></span></code></pre></div><ul><li>Mock：<code>sample/mock/auth.ts</code>（登录）与用户信息接口需带回 <code>roles</code></li><li>注册动态路由前：<code>registerDynamicRoutes</code> 会确保 <code>userStore</code> 有用户信息，再取 <code>roles.map(r =&gt; r.value)</code></li></ul><div class="warning custom-block"><p class="custom-block-title">没有 roles 会怎样？</p><p>FRONT / MIXTURE 会对前端树做过滤。若 <code>roles</code> 为空或缺失，白名单交集为空，<strong>前端菜单可能被滤空</strong>（MIXTURE 仍可能只剩后端树）。</p></div><h3 id="白名单表" tabindex="-1">白名单表 <a class="header-anchor" href="#白名单表" aria-label="Permalink to &quot;白名单表&quot;">​</a></h3><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#676E95", "font-style": "italic" })}">// DesignCornerstone/cornerstone-apps-feat/src/routes/authority.ts</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">export</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#C792EA" })}">const</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> FEAT_ROUTE_AUTHORITY</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#FFCB6B" })}">Record</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&lt;</span><span style="${ssrRenderStyle({ "color": "#FFCB6B" })}">string</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#FFCB6B" })}">string</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">[]</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&gt;</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">=</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">FeatCatalog</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> [</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">super</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">minor</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">]</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">OpenSubpage</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> [</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">super</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">]</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">MixtureDemoCatalog</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> [</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">super</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">minor</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">]</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">MixtureFrontDemo</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> [</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">super</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">minor</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">]</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">  </span><span style="${ssrRenderStyle({ "color": "#F07178" })}">Child</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> [</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">super</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">]</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#676E95", "font-style": "italic" })}">// 隐藏子路由也要配</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span></span></code></pre></div><p>规则（与参考项目一致）：</p><table><thead><tr><th>规则</th><th>行为</th></tr></thead><tbody><tr><td>name 未配置</td><td><strong>无权限</strong></td></tr><tr><td>叶子节点</td><td><code>roles</code> 与白名单有交集才保留</td></tr><tr><td>有 children</td><td>先过滤子级；子级非空则保留父级（即使父级自身未匹配也可因「还有子菜单」保留）</td></tr></tbody></table><p>工具函数：<code>filterConfigsByRoles</code>、<code>canAccessRouteByRoles</code>。</p><p><strong>BACK 模式</strong>不使用该表过滤接口菜单。隐藏路由 <code>FEAT_HIDDEN_ROUTES</code> 在 FRONT / MIXTURE 下会按角色过滤；BACK 下默认注册隐藏路由（不做角色裁剪）。</p><h2 id="前端路由表-如何开发-front-mixture" tabindex="-1">前端路由表：如何开发（FRONT / MIXTURE） <a class="header-anchor" href="#前端路由表-如何开发-front-mixture" aria-label="Permalink to &quot;前端路由表：如何开发（FRONT / MIXTURE）&quot;">​</a></h2><p>前端完整配置 = <strong>展示信息</strong> + <strong>结构</strong>，由 <code>toFeatRouteConfigs()</code> 合并。</p><h3 id="相关文件" tabindex="-1">相关文件 <a class="header-anchor" href="#相关文件" aria-label="Permalink to &quot;相关文件&quot;">​</a></h3><table><thead><tr><th>文件</th><th>职责</th></tr></thead><tbody><tr><td><code>apps-feat/src/routes/menuList.ts</code></td><td><code>FEAT_MENU_LIST</code>：title / icon / sort / 可见性（可与 BACK Mock 共用）</td></tr><tr><td>同上</td><td><code>FEAT_FRONT_ONLY_MENU_LIST</code>：仅前端展示项</td></tr><tr><td><code>apps-feat/src/routes/config.ts</code></td><td><code>FEAT_ROUTE_STRUCTURES</code> / <code>FEAT_FRONT_ONLY_STRUCTURES</code>：path、componentKey</td></tr><tr><td><code>apps-feat/src/routes/mergeMenu.ts</code></td><td><code>mergeFeatMenuWithStructure</code> / <code>toFeatRouteConfigs</code></td></tr><tr><td><code>apps-feat/src/routes/authority.ts</code></td><td>角色白名单</td></tr><tr><td><code>apps-feat/src/routes/index.ts</code></td><td>组件映射、<code>resolveFeatRoute</code></td></tr><tr><td><code>apps-feat/src/pages/**</code></td><td>页面组件</td></tr></tbody></table><h3 id="新增一个需鉴权的前端页面-推荐步骤" tabindex="-1">新增一个需鉴权的前端页面（推荐步骤） <a class="header-anchor" href="#新增一个需鉴权的前端页面-推荐步骤" aria-label="Permalink to &quot;新增一个需鉴权的前端页面（推荐步骤）&quot;">​</a></h3><ol><li><strong>写页面</strong> — <code>apps-feat/src/pages/xxx/xxx.vue</code>（组件 <code>name</code> 需与缓存约定一致时，再配 <code>componentName</code> 映射）</li><li><strong>结构</strong> — 在 <code>FEAT_ROUTE_STRUCTURES</code>（或 <code>FEAT_FRONT_ONLY_STRUCTURES</code>）增加：</li></ol><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#F07178" })}">  </span><span style="${ssrRenderStyle({ "color": "#FFCB6B" })}">path</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">my-page</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#F07178" })}">  </span><span style="${ssrRenderStyle({ "color": "#FFCB6B" })}">name</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">MyPage</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#F07178" })}">  </span><span style="${ssrRenderStyle({ "color": "#FFCB6B" })}">componentKey</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">:</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">MyPage</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span></span></code></pre></div><ol start="3"><li><strong>展示</strong> — 在对应 <code>menuList</code> 增加 title、icon、<code>menuType</code>、<code>sort</code> 等</li><li><strong>映射</strong> — <code>routes/index.ts</code> 的 components 表：<code>MyPage: () =&gt; import(&#39;...&#39;)</code></li><li><strong>权限</strong> — <code>FEAT_ROUTE_AUTHORITY.MyPage = [&#39;super&#39;]</code></li><li><strong>验证</strong> — <code>permissionMode</code> 设为 <code>FRONT</code> 或 <code>MIXTURE</code>，用带对应 <code>roles.value</code> 的账号登录</li></ol><div class="warning custom-block"><p class="custom-block-title">侧栏只显示 name？</p><p>FRONT 必须走 <code>toFeatRouteConfigs()</code>（合并 menuList 的 title/icon），不要只拿 structure 填默认 title。合并失败（menu 与 structure 的 name 对不上）会直接抛错。</p></div><h2 id="后端菜单-如何开发-back-mixture" tabindex="-1">后端菜单：如何开发（BACK / MIXTURE） <a class="header-anchor" href="#后端菜单-如何开发-back-mixture" aria-label="Permalink to &quot;后端菜单：如何开发（BACK / MIXTURE）&quot;">​</a></h2><p>与原先动态路由一致，只是「谁消费」由模式决定：</p><ol><li>业务包维护可序列化配置（如 <code>apps-workspace/src/routes/config.ts</code>）+ 组件映射（<code>index.ts</code>）</li><li>Mock 或真实接口 <code>GET /api/menu/list</code> 返回树</li><li><code>registerDynamicRoutes</code> 在 BACK / MIXTURE 中 <code>fetchBackConfigs()</code> 拉取</li></ol><p>MOCK 示例：<code>sample/mock/routers.ts</code> / <code>sample/mock/menuList.ts</code>。</p><p>新增后端独有页（如 <code>MixtureBackDemo</code>）：只加进 workspace 配置与 Mock，<strong>不要</strong>放进 <code>FEAT_FRONT_ONLY_*</code>。</p><h2 id="侧栏与默认首页怎么读菜单" tabindex="-1">侧栏与默认首页怎么读菜单 <a class="header-anchor" href="#侧栏与默认首页怎么读菜单" aria-label="Permalink to &quot;侧栏与默认首页怎么读菜单&quot;">​</a></h2><h3 id="取当前生效菜单" tabindex="-1">取当前生效菜单 <a class="header-anchor" href="#取当前生效菜单" aria-label="Permalink to &quot;取当前生效菜单&quot;">​</a></h3><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">import</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">useAuthMenuList</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">,</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">useAuthStore</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">from</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">@grow-admin-rock/state</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#C792EA" })}">const</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> menus </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">=</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">useAuthMenuList</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">()          </span><span style="${ssrRenderStyle({ "color": "#676E95", "font-style": "italic" })}">// 响应式</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#C792EA" })}">const</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> list </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">=</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">useAuthStore</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">()</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">getMenuList  </span><span style="${ssrRenderStyle({ "color": "#676E95", "font-style": "italic" })}">// getter</span></span></code></pre></div><p>内部 <code>resolveActiveMenuList</code>：</p><ul><li>BACK → <code>sortTreesBySort(backMenuList)</code></li><li>FRONT → <code>sortTreesBySort(frontMenuList)</code></li><li>MIXTURE → <code>mergeTreesByName(frontMenuList, backMenuList)</code></li></ul><p>布局菜单组件应使用上述 API，而不是写死读 <code>backMenuList</code>。</p><h3 id="默认打开页" tabindex="-1">默认打开页 <a class="header-anchor" href="#默认打开页" aria-label="Permalink to &quot;默认打开页&quot;">​</a></h3><p>注册结束后 <code>registerHomeIndexRedirect(menus)</code>：</p><ol><li>优先 <code>defaultShow: true</code> 的可导航菜单（<code>menuType === MENU</code>，path 以 <code>/</code> 开头，非浏览器新开）</li><li>否则：第一个<strong>目录</strong>向下找第一个可导航叶子</li><li>再否则：整树回退第一个可导航菜单</li></ol><p>实现：<code>resolveDefaultMenuRedirect</code>（<code>rock-state/tabStore.ts</code>）。可在菜单配置里设 <code>defaultShow: true</code> 指定首页。</p><h2 id="与路由守卫的协作" tabindex="-1">与路由守卫的协作 <a class="header-anchor" href="#与路由守卫的协作" aria-label="Permalink to &quot;与路由守卫的协作&quot;">​</a></h2><p><code>apps-home/src/routes/guard.ts</code>：</p><table><thead><tr><th>场景</th><th>行为</th></tr></thead><tbody><tr><td>未登录访问受保护页</td><td>跳转 Login，带 <code>redirect</code></td></tr><tr><td>已登录且动态路由未注册</td><td><code>registerDynamicRoutes()</code> → 标记已注册</td></tr><tr><td>本次注册检测到模式变更，或 <code>to</code> 不可达</td><td><code>next({ name: &#39;Home&#39;, replace: true })</code></td></tr><tr><td>否则</td><td><code>next</code> 原目标（replace 重试一次以应用新路由表）</td></tr></tbody></table><p>「不可达」判定：resolve 后的末级匹配不能是 <code>Home</code> / <code>HomeIndexRedirect</code> / <code>Login</code> 等空壳。</p><h2 id="关键文件索引" tabindex="-1">关键文件索引 <a class="header-anchor" href="#关键文件索引" aria-label="Permalink to &quot;关键文件索引&quot;">​</a></h2><table><thead><tr><th>文件</th><th>职责</th></tr></thead><tbody><tr><td><code>rock-constants/src/appEnum.ts</code></td><td><code>PermissionModeEnum</code></td></tr><tr><td><code>sample/src/projectSetting.ts</code></td><td>配置入口</td></tr><tr><td><code>sample/src/initAppConfig.ts</code></td><td>启动同步与模式变更清缓存</td></tr><tr><td><code>apps-home/.../registerDynamicRoutes.ts</code></td><td>按模式注册路由 / 写菜单</td></tr><tr><td><code>apps-home/.../guard.ts</code></td><td>触发注册；防白屏回退 Home</td></tr><tr><td><code>apps-feat/.../authority.ts</code></td><td>前端角色白名单</td></tr><tr><td><code>apps-feat/.../mergeMenu.ts</code></td><td><code>toFeatRouteConfigs</code></td></tr><tr><td><code>rock-state/.../authStore.ts</code></td><td><code>back</code>/<code>front</code> 列表与 <code>useAuthMenuList</code></td></tr><tr><td><code>rock-state/.../mergeTreesByName.ts</code></td><td>合集与 sort</td></tr><tr><td><code>rock-state/.../permissionModeCache.ts</code></td><td>模式缓存</td></tr><tr><td><code>middleware-router/.../MenuState.ts</code></td><td><code>isBackMode</code> / <code>isFrontMode</code> / <code>isMixtureMode</code></td></tr></tbody></table><h2 id="开发自检清单" tabindex="-1">开发自检清单 <a class="header-anchor" href="#开发自检清单" aria-label="Permalink to &quot;开发自检清单&quot;">​</a></h2><ol><li>分别将 <code>permissionMode</code> 设为 BACK / FRONT / MIXTURE，刷新后侧栏与可访问 URL 符合上表预期。</li><li>FRONT：改 <code>FEAT_ROUTE_AUTHORITY</code> 去掉某角色后，该菜单消失；未配置的 name 不可见。</li><li>MIXTURE：能同时看到「仅前端」「仅后端」演示项；同名节点展示与 path 以后端为准。</li><li>从 BACK 改到 FRONT（或反之）后，旧标签 / 旧 URL 不应白屏，应回到默认首页。</li><li>用户信息含 <code>roles[].value</code>；缺 roles 时 FRONT 前端树为空可复现并理解原因。</li><li>新增页：structure / menuList / 组件映射 / authority（如需）四处对齐，name 字符串一致。</li></ol><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><ul><li><a href="/guide/architecture/routing-and-menu">路由与菜单</a> — 静态 / 动态路由与菜单树</li><li><a href="/guide/development/authentication">认证与登录</a> — Token、守卫、登录流程</li><li><a href="/guide/development/project-setting">项目配置</a> — <code>projectSetting</code> 全量字段</li><li><a href="/guide/development/mock">Mock 数据</a> — <code>/menu/list</code> 与登录 Mock</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/development/permission-mode.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const permissionMode = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  permissionMode as default
};
