import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.cc2b3d55.js";
const __pageData = JSON.parse('{"title":"数据清洗","description":"","frontmatter":{"title":"数据清洗","lang":"zh-CN"},"headers":[],"relativePath":"data-clean/index.md"}');
const _sfc_main = { name: "data-clean/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="数据清洗" tabindex="-1">数据清洗 <a class="header-anchor" href="#数据清洗" aria-label="Permalink to &quot;数据清洗&quot;">​</a></h1><p>基于 <strong>Vue Flow</strong> 的可视化数据清洗：用声明式 <strong>清洗流（CleanFlow）</strong> 把数据源、清洗算子、合并、聚合与输出串成有向图；流定义在设计期编排，<strong>调用时再执行</strong>（按需跑通整条管道）。设计器内可用 <strong>本地管道预览</strong>（<code>runCleanFlowLocal</code> + Demo / Mock 表数据）验证拓扑与算子效果。</p><table><thead><tr><th>项</th><th>说明</th></tr></thead><tbody><tr><td>核心包</td><td><code>@grow-admin-rock/data-clean</code></td></tr><tr><td>源码目录</td><td><code>DesignRock/rock-data-clean</code></td></tr><tr><td>演示模块</td><td><code>@grow-admin-cornerstone/apps-designer</code>（侧栏菜单：<strong>设计器 → 数据清洗</strong>）</td></tr><tr><td>主要组件</td><td><code>GrowDataCleanDesigner</code></td></tr><tr><td>当前范围</td><td>可视化编排 + <strong>全量节点配置 UI</strong> + <strong>本地管道预览</strong> + Mock <code>/data-clean/*</code>；尚未对接真实建模 / Dataset API 与展示侧消费者绑定</td></tr></tbody></table><p>与其它低代码工具的分工（完整链路见 <a href="/guide/designers/">低代码设计器</a>）：</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">数据库建模（PostgreSQL 物理模型）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    ↓ 已发布元数据</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">数据准备 Dataset（怎么算：Join + 公式度量）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    ↓ 原始表 / 查询输出（亦可直接引用建模表）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">数据清洗 CleanFlow（怎么洗：源 → 算子 → 输出）   ← 本模块</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    ↓ 调用时执行（目标：报表 / 页面 / API，后续对接）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    ├─→ 低代码设计器（页面数据源）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    └─→ 报表设计器（图表数据集）</span></span></code></pre></div><table><thead><tr><th>模块</th><th>定位</th></tr></thead><tbody><tr><td><a href="/schema-designer/">数据库建模</a></td><td>物理模型：表 / 字段 / 关系</td></tr><tr><td><a href="/data-prep/">数据准备</a></td><td>分析模型：选表、Join、公式度量、输出字段</td></tr><tr><td><strong>数据清洗</strong></td><td>ETL 流：多源接入、行级清洗、合并与聚合、声明式输出</td></tr><tr><td><a href="/page-designer/">页面设计器</a> / <a href="/report-designer/">报表设计器</a></td><td>展示：消费清洗输出（经调用时执行，后续对接）</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">与「数据准备」怎么选？</p><ul><li><strong>数据准备</strong>：面向分析查询——维度 / 公式度量、输出投影，结果写入页面 <code>state</code> 再绑图表。</li><li><strong>数据清洗</strong>：面向管道编排——空值、去重、过滤、分支、Join/Union、分组透视等算子图；适合可复用的清洗流水线。 二者可衔接：清洗流的「数据表」源可指向建模表、Dataset 原始表或 Dataset 输出。</li></ul></div><h2 id="界面分区" tabindex="-1">界面分区 <a class="header-anchor" href="#界面分区" aria-label="Permalink to &quot;界面分区&quot;">​</a></h2><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">┌─────────────────────────────────────────────────────────────┐</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ 工具栏：清洗流名称 / 草稿|已发布 / 预览 / 保存                 │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">├──────────┬──────────────────────────────────────────────────┤</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ 组件库   │ Vue Flow 画布                                     │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ 240px    │ 节点、连线、缩放；选中后浮层配置面板                 │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│          │                                                   │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">├──────────┴──────────────────────────────────────────────────┤</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ 底部：管道预览表（选中节点或全流至输出；含 warning / error）     │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">└─────────────────────────────────────────────────────────────┘</span></span></code></pre></div><table><thead><tr><th>区域</th><th>能力</th></tr></thead><tbody><tr><td><strong>工具栏</strong></td><td>改名称；状态标签；预览（有选中 → 该节点；无选中 → 跑至输出）；保存（抛出 <code>save</code>）</td></tr><tr><td><strong>组件库</strong></td><td>按类别折叠：数据源 / 清洗 / 合并 / 聚合 / 输出；拖入画布</td></tr><tr><td><strong>画布</strong></td><td>拖放节点、右侧拖出接到左侧、一对多 / 多对一；Delete 删节点 / 边；<strong>仅允许一个输出节点</strong></td></tr><tr><td><strong>配置浮层</strong></td><td>选中节点后在画布上浮出，编辑名称与节点专属配置（十五种均已有表单）</td></tr><tr><td><strong>预览条</strong></td><td><code>runCleanFlowLocal</code> 按拓扑跑数（Demo / Mock 表），默认最多 50 行</td></tr></tbody></table><h2 id="核心能力" tabindex="-1">核心能力 <a class="header-anchor" href="#核心能力" aria-label="Permalink to &quot;核心能力&quot;">​</a></h2><ol><li><strong>声明式清洗流</strong>：产物为 <code>CleanFlow</code>（<code>version: 1</code>），含 <code>nodes</code> + <code>edges</code>；设计期存定义，生产侧按「调用时执行」消费（对接中）</li><li><strong>五类算子组件库</strong>：源（表 / API）、清洗（空值、格式、去重、过滤、分支、字段拆分等）、合并（Join / Union）、聚合（分组 / 透视）、输出</li><li><strong>本地管道执行</strong>：<code>runCleanFlowLocal</code> 对全部节点类型做真实变换；设计器预览与 Mock <code>POST /data-clean/preview</code> 共用该引擎</li><li><strong>有向无环连线</strong>：必须从输出锚点拖到输入锚点；禁止自连与成环；同对同出口不重复连边</li><li><strong>条件分支双出口</strong>：<code>condition</code> 节点右侧「是 / 否」两路，与「条件过滤」（丢弃行）语义不同</li><li><strong>多源表引用（Demo）</strong>：建模表、Dataset 原始表、Dataset 输出三类来源（列表为前端 / Mock 写死）</li></ol><h2 id="当前边界-务必知晓" tabindex="-1">当前边界（务必知晓） <a class="header-anchor" href="#当前边界-务必知晓" aria-label="Permalink to &quot;当前边界（务必知晓）&quot;">​</a></h2><table><thead><tr><th>已具备</th><th>尚未具备（后续版本）</th></tr></thead><tbody><tr><td>拖拽编排、连线校验、删除快捷键</td><td>发布状态切换 UI</td></tr><tr><td>十五种节点配置 UI + 本地变换</td><td>真实建模 / Dataset 远程拉表</td></tr><tr><td>Mock \`/data-clean/tables</td><td>table-rows</td></tr><tr><td><code>getFlow</code> / <code>setFlow</code> 受控 API</td><td>生产「调用时执行」服务端编排</td></tr></tbody></table><h2 id="推荐阅读" tabindex="-1">推荐阅读 <a class="header-anchor" href="#推荐阅读" aria-label="Permalink to &quot;推荐阅读&quot;">​</a></h2><ol><li><a href="/data-clean/usage">基础用法</a> — 接入演示、工具栏 / 画布操作、组件 API</li><li><a href="/data-clean/schema">数据模型</a> — <code>CleanFlow</code> / 节点 config / 工厂与导出</li><li><a href="/data-clean/nodes">节点与算子</a> — 类别、端口、各类型配置字段与过滤运算符</li></ol><div class="tip custom-block"><p class="custom-block-title">演示入口</p><p>登录后打开侧栏 <strong>设计器 → 数据清洗</strong>（由 <code>apps-designer</code> 注册）。本地需装配 <code>@grow-admin-rock/data-clean</code>、对应 Cornerstone 模块，并启用 <code>sample/mock/dataClean.ts</code>。</p></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("data-clean/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
