import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.cc2b3d55.js";
const __pageData = JSON.parse('{"title":"架构设计理念","description":"","frontmatter":{"title":"架构设计理念","lang":"zh-CN"},"headers":[],"relativePath":"guide/architecture/design-philosophy.md"}');
const _sfc_main = { name: "guide/architecture/design-philosophy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="架构设计理念" tabindex="-1">架构设计理念 <a class="header-anchor" href="#架构设计理念" aria-label="Permalink to &quot;架构设计理念&quot;">​</a></h1><p>Grow Admin 不是「把 Vue Admin 模板拆成几个文件夹」，而是一套围绕<strong>可组合、可替换、可演进</strong>目标设计的 Monorepo 框架。本章说明核心设计决策背后的动机，以及这些决策带来的长期收益。</p><p>阅读本章后，你会更容易理解：为什么要有 Rock / Cornerstone 分层、为什么要 IOC、为什么要组件驱动、为什么路由要动静分离。</p><h2 id="设计目标" tabindex="-1">设计目标 <a class="header-anchor" href="#设计目标" aria-label="Permalink to &quot;设计目标&quot;">​</a></h2><table><thead><tr><th>目标</th><th>含义</th></tr></thead><tbody><tr><td><strong>框架与业务解耦</strong></td><td>换业务不换框架，换框架少动业务</td></tr><tr><td><strong>模块可独立演进</strong></td><td>登录、首页、工作区可单独开发、测试、发版</td></tr><tr><td><strong>技术选型可替换</strong></td><td>UI 库、HTTP 层、宿主部署方式可在边界处替换</td></tr><tr><td><strong>约定优于配置</strong></td><td>Library 契约、Grow* 组件、路由 meta 降低协作成本</td></tr></tbody></table><h2 id="与传统-admin-模板的对比" tabindex="-1">与传统 Admin 模板的对比 <a class="header-anchor" href="#与传统-admin-模板的对比" aria-label="Permalink to &quot;与传统 Admin 模板的对比&quot;">​</a></h2><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">传统单应用 Admin                    Grow Admin</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">─────────────────                  ─────────────────</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">src/views/ 全部堆在一起      →      DesignCornerstone 按业务能力分包</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">直接 import Element Plus     →      Grow* 契约 + 驱动桥接</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">main.ts 手写注册路由         →      Library.routes + 动态注册</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">utils/request.ts 全局单例    →      IOC 注入 InfrastructureAxios</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">改一个页面可能影响全局       →      依赖方向单向，边界清晰</span></span></code></pre></div><p>传统模板适合快速出 Demo；Grow Admin 适合<strong>多项目复用、多团队并行、长期维护</strong>的中后台场景。</p><hr><h2 id="一、monorepo-四层分离" tabindex="-1">一、Monorepo 四层分离 <a class="header-anchor" href="#一、monorepo-四层分离" aria-label="Permalink to &quot;一、Monorepo 四层分离&quot;">​</a></h2><h3 id="为什么这样设计" tabindex="-1">为什么这样设计？ <a class="header-anchor" href="#为什么这样设计" aria-label="Permalink to &quot;为什么这样设计？&quot;">​</a></h3><p>中后台项目往往同时存在三类变化：</p><ol><li><strong>框架能力变化</strong>（路由中间件、主题、国际化、组件桥接）</li><li><strong>业务能力变化</strong>（登录、审批、报表、工作流）</li><li><strong>部署环境变化</strong>（不同客户、不同 API、不同 UI 规范）</li></ol><p>若全部写在一个 <code>src/</code> 里，三者会缠在一起：改主题可能误触业务路由，换 UI 库要全局搜索 <code>el-</code> 前缀。</p><p>Grow Admin 用四层职责切开：</p><table><thead><tr><th>层级</th><th>职责</th><th>变化频率</th></tr></thead><tbody><tr><td><code>configs</code></td><td>构建与工具链</td><td>低</td></tr><tr><td><code>DesignRock</code></td><td>与业务无关的框架能力</td><td>中</td></tr><tr><td><code>DesignCornerstone</code></td><td>可复用的业务模块</td><td>高</td></tr><tr><td><code>sample</code></td><td>宿主：选型、装配、环境配置</td><td>按项目</td></tr></tbody></table><p><strong>依赖方向单向</strong>：<code>sample → Cornerstone → Rock → configs</code>，禁止反向依赖。这样 Rock 包永远不会 import 某个具体业务页面。</p><h3 id="带来的好处" tabindex="-1">带来的好处 <a class="header-anchor" href="#带来的好处" aria-label="Permalink to &quot;带来的好处&quot;">​</a></h3><ul><li><strong>框架可独立发版</strong>：升级 <code>rock-components</code> 不必改业务源码</li><li><strong>业务可跨项目复用</strong>：<code>apps-login</code> 可装配进不同宿主</li><li><strong>职责清晰</strong>：新人看目录就知道该改哪一层</li><li><strong>CI 可分层构建</strong>：Turbo 按包增量编译</li></ul><h3 id="宿主角色的意义" tabindex="-1">宿主角色的意义 <a class="header-anchor" href="#宿主角色的意义" aria-label="Permalink to &quot;宿主角色的意义&quot;">​</a></h3><p><code>sample</code> 不是「示例代码随便写写」，而是<strong>组合根（Composition Root）</strong>：</p><ul><li>选哪个 UI 库 → <code>projectSetting</code> + <code>init-components-driver</code></li><li>装哪些模块 → <code>initIoc.ts</code></li><li>环境差异 → <code>.env</code>、<code>GrowAxiosTransform</code></li></ul><p>框架故意把「选型」放在宿主，而不是写死在 Rock 里——<strong>同一个 Rock，可以长出不同风格的管理后台</strong>。</p><hr><h2 id="二、ioc-模块化-library-契约" tabindex="-1">二、IOC 模块化（Library 契约） <a class="header-anchor" href="#二、ioc-模块化-library-契约" aria-label="Permalink to &quot;二、IOC 模块化（Library 契约）&quot;">​</a></h2><h3 id="为什么不用「到处-import」" tabindex="-1">为什么不用「到处 import」？ <a class="header-anchor" href="#为什么不用「到处-import」" aria-label="Permalink to &quot;为什么不用「到处 import」？&quot;">​</a></h3><p>单应用里常见写法：</p><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">import</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> router </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">from</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">./router</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">import</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">http</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">from</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">./utils/request</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">import</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> Login </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">from</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">./views/login.vue</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span></span></code></pre></div><p>问题是：模块之间通过<strong>文件路径</strong>耦合，拆包后路径断裂；测试时难以替换实现；启动顺序藏在各个 <code>import</code> 的副作用里，无人知晓全貌。</p><p>Grow Admin 采用 <strong>Library + AppContext + Inversify</strong>：</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">各模块声明 Lib（routes / module / onSetup）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">        ↓</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">宿主 app.use(Lib, appContext) 收集能力</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">        ↓</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">appContext.load() 统一加载 IOC + 执行生命周期</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">        ↓</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">diKT(Beans.Xxx) 按标识符获取服务</span></span></code></pre></div><h3 id="为什么需要-appcontext" tabindex="-1">为什么需要 AppContext？ <a class="header-anchor" href="#为什么需要-appcontext" aria-label="Permalink to &quot;为什么需要 AppContext？&quot;">​</a></h3><p>IOC 容器解决「服务从哪来」，AppContext 解决「应用级编排」：</p><ul><li>汇总所有模块的 <code>routes</code>，再交给 <code>middleware-router</code> 建表</li><li>收集 <code>iocModules</code>，一次性 <code>loadAsync</code></li><li><code>registerParam</code> 传递跨模块参数（如 <code>DriverComponentDictionary</code>）</li><li><code>beforeSetup</code> / <code>onSetup</code> 有序执行（支持 priority）</li></ul><p><strong>一次 <code>load()</code>，全局就绪</strong>——避免「A 模块 onMounted 时 B 模块还没注册」的竞态。</p><h3 id="带来的好处-1" tabindex="-1">带来的好处 <a class="header-anchor" href="#带来的好处-1" aria-label="Permalink to &quot;带来的好处&quot;">​</a></h3><table><thead><tr><th>收益</th><th>说明</th></tr></thead><tbody><tr><td><strong>可装配</strong></td><td>不需要的模块不 <code>.use()</code> 即可</td></tr><tr><td><strong>可测试</strong></td><td>IOC 里换 Mock 实现，无需改业务 import</td></tr><tr><td><strong>可观测</strong></td><td><code>initIoc.ts</code> 即应用能力清单</td></tr><tr><td><strong>可扩展</strong></td><td>新业务包只需实现 Library 契约</td></tr></tbody></table><h3 id="装配顺序为何敏感" tabindex="-1">装配顺序为何敏感？ <a class="header-anchor" href="#装配顺序为何敏感" aria-label="Permalink to &quot;装配顺序为何敏感？&quot;">​</a></h3><p>组件驱动 → 基础设施 → 状态/语言 → 路由 → 业务 → 契约组件，不是随意排列：</p><ul><li>驱动必须先于 <code>componentsLib.onSetup</code>，否则 Grow 组件找不到映射</li><li>路由中间件先于业务模块，业务 <code>routes</code> 才能汇入路由表</li><li><code>router</code> 在 <code>load()</code> 之后由宿主挂载，保证守卫注册时容器已就绪</li></ul><p>顺序即<strong>架构文档</strong>，写死在 <code>initIoc.ts</code> 是有意为之。</p><hr><h2 id="三、组件驱动桥接-grow-契约" tabindex="-1">三、组件驱动桥接（Grow* 契约） <a class="header-anchor" href="#三、组件驱动桥接-grow-契约" aria-label="Permalink to &quot;三、组件驱动桥接（Grow* 契约）&quot;">​</a></h2><h3 id="为什么要多一层-而不是直接用-element-plus" tabindex="-1">为什么要多一层，而不是直接用 Element Plus？ <a class="header-anchor" href="#为什么要多一层-而不是直接用-element-plus" aria-label="Permalink to &quot;为什么要多一层，而不是直接用 Element Plus？&quot;">​</a></h3><p>中后台项目生命周期长，UI 库选型常变：</p><ul><li>团队熟悉度、设计规范、组件缺失、License、包体积……</li></ul><p>若在几百个 <code>.vue</code> 里直接写 <code>&lt;el-button&gt;</code>，换库成本 ≈ 重写前端。抽象太早（大而全的二次封装）又会导致 API 失真、维护地狱。</p><p>Grow Admin 选择 <strong>「契约组件 + 驱动映射」</strong> 的中间路线：</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">业务只认识 GrowButton</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">        ↓</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">契约层保持统一 Props / 插槽约定</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">        ↓</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">驱动层把 GrowButton 映射到 ElButton / NButton / AButton</span></span></code></pre></div><p>只抽象三库<strong>共有的 84 个组件</strong>，不虚构不存在的 API。</p><h3 id="为什么切换在宿主-而不是业务模块" tabindex="-1">为什么切换在宿主，而不是业务模块？ <a class="header-anchor" href="#为什么切换在宿主-而不是业务模块" aria-label="Permalink to &quot;为什么切换在宿主，而不是业务模块？&quot;">​</a></h3><p>若每个业务包自己 <code>import element-plus</code>，会出现：</p><ul><li>A 模块用 EP，B 模块用 Naive，全局样式冲突</li><li>业务包无法作为纯净 npm 包发布</li><li>树摇与按需加载策略无法统一</li></ul><p><strong>宿主统一 <code>installComponentDriver</code></strong>，业务包 peer 依赖 <code>@grow-admin-rock/components</code> 即可——业务包是「UI 库无关」的。</p><h3 id="componentdriverprovider-解决什么问题" tabindex="-1">ComponentDriverProvider 解决什么问题？ <a class="header-anchor" href="#componentdriverprovider-解决什么问题" aria-label="Permalink to &quot;ComponentDriverProvider 解决什么问题？&quot;">​</a></h3><p>少数页面需要与全局不同的 UI（如嵌入旧系统、A/B 对比）。全局切换不现实，<strong>子树级覆盖</strong>既保留默认一致性，又留出逃生舱。</p><h3 id="带来的好处-2" tabindex="-1">带来的好处 <a class="header-anchor" href="#带来的好处-2" aria-label="Permalink to &quot;带来的好处&quot;">​</a></h3><ul><li><strong>业务代码稳定</strong>：切换 <code>componentLibrary</code> + <code>preset</code>，页面零改动</li><li><strong>契约边界清晰</strong>：84 个组件清单 = 可承诺的兼容范围</li><li><strong>命令式 API 同样统一</strong>：Message / Dialog 经桥接注入，不散落 <code>ElMessage</code></li></ul><hr><h2 id="四、路由与菜单-静态-动态" tabindex="-1">四、路由与菜单：静态 + 动态 <a class="header-anchor" href="#四、路由与菜单-静态-动态" aria-label="Permalink to &quot;四、路由与菜单：静态 + 动态&quot;">​</a></h2><h3 id="为什么分「静态基础路由」和「动态业务路由」" tabindex="-1">为什么分「静态基础路由」和「动态业务路由」？ <a class="header-anchor" href="#为什么分「静态基础路由」和「动态业务路由」" aria-label="Permalink to &quot;为什么分「静态基础路由」和「动态业务路由」？&quot;">​</a></h3><p>中后台路由有两类本质不同的来源：</p><table><thead><tr><th>类型</th><th>来源</th><th>特点</th></tr></thead><tbody><tr><td>基础路由</td><td>前端框架约定</td><td>登录、布局壳——不随权限变</td></tr><tr><td>业务路由</td><td>后端权限 / 菜单</td><td>随用户、租户、角色变化</td></tr></tbody></table><p>全静态：每次加菜单要发版；全动态：登录页、404 等框架页也要走接口，启动慢、体验差。</p><p><strong>静态注册骨架 + 动态注入业务子路由</strong>，各取所长。</p><h3 id="为什么业务路由挂在-home-下" tabindex="-1">为什么业务路由挂在 Home 下？ <a class="header-anchor" href="#为什么业务路由挂在-home-下" aria-label="Permalink to &quot;为什么业务路由挂在 Home 下？&quot;">​</a></h3><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">/home          ← 布局壳（侧栏、顶栏、设置抽屉）只挂载一次</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">/home/xxx      ← 业务页在 &lt;router-view /&gt; 内切换</span></span></code></pre></div><p>避免每个业务页重复布局；权限重置时只清 <code>appRoutes</code>，<code>basicRoutes</code>（Login、Home）保留。</p><h3 id="为什么菜单与路由共用数据、却分开处理" tabindex="-1">为什么菜单与路由共用数据、却分开处理？ <a class="header-anchor" href="#为什么菜单与路由共用数据、却分开处理" aria-label="Permalink to &quot;为什么菜单与路由共用数据、却分开处理？&quot;">​</a></h3><p>同一份 <code>menuList</code> 有两种消费方式：</p><table><thead><tr><th>消费者</th><th>需要的数据形态</th><th>原因</th></tr></thead><tbody><tr><td>Vue Router</td><td>扁平叶子路由</td><td>路由器不认识「纯目录节点」</td></tr><tr><td>侧边菜单</td><td>树形结构</td><td>用户需要折叠目录</td></tr></tbody></table><p><code>flatten</code> 注册路由 + <code>toMenuList</code> 保留树——<strong>一次请求，两种视图</strong>，避免菜单接口与路由接口不一致。</p><h3 id="为什么配置与组件映射分离" tabindex="-1">为什么配置与组件映射分离？ <a class="header-anchor" href="#为什么配置与组件映射分离" aria-label="Permalink to &quot;为什么配置与组件映射分离？&quot;">​</a></h3><p><code>apps-workspace/route-config</code> 不含 <code>.vue</code>：</p><ul><li>Mock / 后端 API 只返回 JSON 元数据</li><li>vite-plugin-mock 不会误打包页面组件</li><li>组件映射留在前端 <code>resolveWorkspaceRoute()</code>，安全且灵活</li></ul><p>这是<strong>前后端职责分界</strong>：后端管「谁能看什么」，前端管「页面长什么样」。</p><h3 id="为什么通过-ioc-拿-router" tabindex="-1">为什么通过 IOC 拿 router？ <a class="header-anchor" href="#为什么通过-ioc-拿-router" aria-label="Permalink to &quot;为什么通过 IOC 拿 router？&quot;">​</a></h3><p><code>rock-layouts</code> 是框架布局包，不应强依赖 <code>vue-router</code> 的 composable 上下文。通过 <code>RouteTable</code> 服务：</p><ul><li>布局包与路由实现解耦</li><li>与 IOC 体系一致，便于测试替换</li><li>动态 <code>addRoute</code> 与菜单跳转共用同一实例</li></ul><h3 id="守卫-bootstrap-双触发" tabindex="-1">守卫 + Bootstrap 双触发 <a class="header-anchor" href="#守卫-bootstrap-双触发" aria-label="Permalink to &quot;守卫 + Bootstrap 双触发&quot;">​</a></h3><table><thead><tr><th>机制</th><th>时机</th><th>目的</th></tr></thead><tbody><tr><td>路由守卫</td><td>导航前</td><td>直链 <code>/home/workspace</code> 时路由必须已存在</td></tr><tr><td><code>useAppBootstrap</code></td><td>Home 挂载后</td><td>页面 Loading、用户信息等体验逻辑</td></tr></tbody></table><p><code>isDynamicAddedRoute</code> 防重复——<strong>正确性</strong>与<strong>体验</strong>分开，不互相牺牲。</p><hr><h2 id="五、横切关注点的设计" tabindex="-1">五、横切关注点的设计 <a class="header-anchor" href="#五、横切关注点的设计" aria-label="Permalink to &quot;五、横切关注点的设计&quot;">​</a></h2><h3 id="主题-css-变量-状态-三库-config" tabindex="-1">主题：CSS 变量 + 状态 + 三库 Config <a class="header-anchor" href="#主题-css-变量-状态-三库-config" aria-label="Permalink to &quot;主题：CSS 变量 + 状态 + 三库 Config&quot;">​</a></h3><p>为什么不用各业务页写死颜色？</p><ul><li>亮/暗切换、主题色、UnoCSS 语义类需要<strong>单一数据源</strong></li><li><code>useTheme</code> 动态写 <code>:root</code>，三库 <code>GrowConfig</code> 同步主色——改一处，全局生效</li></ul><h3 id="http-transform-在宿主扩展" tabindex="-1">HTTP：Transform 在宿主扩展 <a class="header-anchor" href="#http-transform-在宿主扩展" aria-label="Permalink to &quot;HTTP：Transform 在宿主扩展&quot;">​</a></h3><p>框架提供 <code>InfrastructureAxios</code> 管道，宿主注入 <code>GrowAxiosTransform</code>：</p><ul><li>框架不假设你的后端响应格式</li><li>Mock 的 <code>resultSuccess</code> 与 Transform 解包规则对齐——开发生产一致</li></ul><h3 id="mock-开发便利但不污染业务" tabindex="-1">Mock：开发便利但不污染业务 <a class="header-anchor" href="#mock-开发便利但不污染业务" aria-label="Permalink to &quot;Mock：开发便利但不污染业务&quot;">​</a></h3><p>Mock 是宿主能力，业务包通过 <code>route-config</code> 导出纯数据。业务模块不 import mock 文件，<strong>上线时去掉 mock 装配即可</strong>。</p><hr><h2 id="六、设计权衡-诚实说明" tabindex="-1">六、设计权衡（诚实说明） <a class="header-anchor" href="#六、设计权衡-诚实说明" aria-label="Permalink to &quot;六、设计权衡（诚实说明）&quot;">​</a></h2><p>没有银弹。Grow Admin 为上述目标付出了一些成本：</p><table><thead><tr><th>权衡</th><th>我们选择了</th><th>代价</th></tr></thead><tbody><tr><td>抽象 vs 直接</td><td>Grow* 契约层</td><td>新组件需补驱动映射</td></tr><tr><td>灵活 vs 简单</td><td>IOC + Library</td><td>上手需理解装配顺序</td></tr><tr><td>动态 vs 静态</td><td>接口驱动菜单</td><td>需处理守卫时机、直链访问</td></tr><tr><td>统一 vs 自由</td><td>84 个共有组件</td><td>特殊组件需业务自封装</td></tr></tbody></table><p>若项目只有 3～5 个固定页面、团队永远不换 UI 库，传统单应用可能更轻。<strong>当模块数、团队数、项目寿命增长时，这套架构的收益会放大。</strong></p><hr><h2 id="架构全景" tabindex="-1">架构全景 <a class="header-anchor" href="#架构全景" aria-label="Permalink to &quot;架构全景&quot;">​</a></h2><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">┌─────────────────────────────────┐</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    │           sample 宿主            │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    │  选型 · 装配 · 环境 · Mock      │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    └───────────────┬─────────────────┘</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                                    │ .use(Lib)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">          ┌─────────────────────────┼─────────────────────────┐</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">          ▼                         ▼                         ▼</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">   apps-login              apps-home                 apps-workspace</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">   登录/Token              守卫/动态路由              页面/路由配置</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">          │                         │                         │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">          └─────────────────────────┼─────────────────────────┘</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                                    ▼</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    ┌─────────────────────────────────┐</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    │         DesignRock 框架            │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    │  IOC · Grow* · 驱动 · 路由 · 状态  │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    └───────────────┬─────────────────┘</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                                    ▼</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    ┌─────────────────────────────────┐</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    │            configs               │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    │     Vite · TS · UnoCSS · Mock    │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                    └─────────────────────────────────┘</span></span></code></pre></div><h2 id="延伸阅读" tabindex="-1">延伸阅读 <a class="header-anchor" href="#延伸阅读" aria-label="Permalink to &quot;延伸阅读&quot;">​</a></h2><table><thead><tr><th>章节</th><th>内容</th></tr></thead><tbody><tr><td><a href="/guide/architecture/project-structure">项目结构</a></td><td>目录与分层细节</td></tr><tr><td><a href="/guide/architecture/ioc">IOC 模块化</a></td><td>Library 契约与装配</td></tr><tr><td><a href="/guide/architecture/component-driver">组件驱动架构</a></td><td>Grow* 与驱动加载</td></tr><tr><td><a href="/guide/architecture/routing-and-menu">路由与菜单</a></td><td>动态路由与菜单树</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/architecture/design-philosophy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const designPhilosophy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  designPhilosophy as default
};
