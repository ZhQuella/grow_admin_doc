import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.cc2b3d55.js";
const __pageData = JSON.parse('{"title":"DesignRock 核心层","description":"","frontmatter":{"title":"DesignRock 核心层","lang":"zh-CN"},"headers":[],"relativePath":"guide/packages/design-rock.md"}');
const _sfc_main = { name: "guide/packages/design-rock.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="designrock-核心层包说明" tabindex="-1">DesignRock 核心层包说明 <a class="header-anchor" href="#designrock-核心层包说明" aria-label="Permalink to &quot;DesignRock 核心层包说明&quot;">​</a></h1><p><code>DesignRock</code>（磐石）是 Grow Admin 的框架核心层，所有包以 <code>@grow-admin-rock/*</code> 命名。</p><p>README「相关包说明」速查表见 <a href="/guide/development/dev-conventions#相关包速查">开发规范 - 相关包速查</a>。</p><h2 id="包总览" tabindex="-1">包总览 <a class="header-anchor" href="#包总览" aria-label="Permalink to &quot;包总览&quot;">​</a></h2><table><thead><tr><th>包名</th><th>职责</th></tr></thead><tbody><tr><td><code>@grow-admin-rock/ioc</code></td><td>IOC 依赖注入容器（基于 Inversify）</td></tr><tr><td><code>@grow-admin-rock/base-package</code></td><td>Library 基座、<code>AppContext</code>、模块 install 机制</td></tr><tr><td><code>@grow-admin-rock/types</code></td><td>全局 TypeScript 类型定义</td></tr><tr><td><code>@grow-admin-rock/constants</code></td><td>枚举与常量（主题、菜单、权限等）</td></tr><tr><td><code>@grow-admin-rock/components</code></td><td><code>RockComponent</code> 枚举、<code>Grow*</code> 契约组件、<code>ComponentMap</code></td></tr><tr><td><code>@grow-admin-rock/component-driver</code></td><td>抽象驱动、<code>ComponentDriverProvider</code>、Builder API</td></tr><tr><td><code>@grow-admin-rock/component-driver-element-plus</code></td><td>Element Plus 组件映射（84 个）</td></tr><tr><td><code>@grow-admin-rock/component-driver-naive</code></td><td>Naive UI 组件映射（84 个）</td></tr><tr><td><code>@grow-admin-rock/component-driver-antdv</code></td><td>Ant Design Vue 组件映射（84 个）</td></tr><tr><td><code>@grow-admin-rock/layouts</code></td><td>布局壳：<code>Layout</code>、<code>SettingDrawer</code>、<code>Menu</code>、<code>SettingTheme</code>、<code>SwitchLanguage</code> 等</td></tr><tr><td><code>@grow-admin-rock/mock</code></td><td>Mock 注册中心、<code>resultSuccess</code>/<code>resultError</code> 工具</td></tr><tr><td><code>@grow-admin-rock/infrastructure</code></td><td>HTTP 请求基础设施（Axios 封装）</td></tr><tr><td><code>@grow-admin-rock/middleware-router</code></td><td>路由表、菜单状态、路由操作器</td></tr><tr><td><code>@grow-admin-rock/settings</code></td><td>项目设置（主题色、布局模式、暗色模式）</td></tr><tr><td><code>@grow-admin-rock/state</code></td><td>Pinia 状态管理封装</td></tr><tr><td><code>@grow-admin-rock/locale</code></td><td>国际化（i18n）</td></tr><tr><td><code>@grow-admin-rock/hooks</code></td><td>通用 Hooks（设置、断点等）</td></tr><tr><td><code>@grow-admin-rock/utils</code></td><td>工具函数（含 VueUse 重导出）</td></tr><tr><td><code>@grow-admin-rock/styles</code></td><td>全局样式与 Reset CSS</td></tr><tr><td><code>@grow-admin-rock/code-sandbox</code></td><td>在线代码编辑与 Vue SFC 沙箱预览</td></tr><tr><td><code>@grow-admin-rock/designer</code></td><td>低代码页面设计器（<code>GrowDesigner</code> / <code>GrowRenderer</code>）</td></tr><tr><td><code>@grow-admin-rock/report-designer</code></td><td>报表设计器（<code>GrowReportDesigner</code> / <code>GrowReportRenderer</code>）</td></tr><tr><td><code>@grow-admin-rock/schema-designer</code></td><td>可视化数据库建模（<code>GrowSchemaDesigner</code>）</td></tr><tr><td><code>@grow-admin-rock/data-prep</code></td><td>数据准备 Dataset（<code>GrowDataPrepDesigner</code>）</td></tr><tr><td><code>@grow-admin-rock/data-clean</code></td><td>数据清洗流（<code>GrowDataCleanDesigner</code>）</td></tr><tr><td><code>@grow-admin-rock/process-engine</code></td><td>流程引擎（<code>GrowProcessDesigner</code>）</td></tr></tbody></table><hr><h2 id="grow-admin-rock-ioc" tabindex="-1">@grow-admin-rock/ioc <a class="header-anchor" href="#grow-admin-rock-ioc" aria-label="Permalink to &quot;@grow-admin-rock/ioc&quot;">​</a></h2><p>基于 Inversify 的 IOC 容器封装。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>IocPlugin</code></td><td>Vue 插件，挂载 IOC 容器</td></tr><tr><td><code>IocContainer</code></td><td>IOC 容器类</td></tr><tr><td><code>AsyncIocModule</code></td><td>异步模块加载</td></tr><tr><td><code>diKT</code> / <code>di</code></td><td>服务获取工具</td></tr><tr><td><code>THROWN_HANDLER</code></td><td>异常处理标识</td></tr></tbody></table><hr><h2 id="grow-admin-rock-base-package" tabindex="-1">@grow-admin-rock/base-package <a class="header-anchor" href="#grow-admin-rock-base-package" aria-label="Permalink to &quot;@grow-admin-rock/base-package&quot;">​</a></h2><p>Library 基座，定义模块接入契约。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>AppContext</code></td><td>应用上下文（路由、IOC 模块、参数、生命周期）</td></tr><tr><td><code>install</code></td><td>Library 自动装配函数</td></tr><tr><td><code>toPackage</code></td><td>将配置转为标准 Library</td></tr><tr><td><code>Library</code> / <code>ModuleLibContext</code></td><td>Library 类型定义</td></tr><tr><td><code>APP_CONTEXT</code></td><td>IOC 服务标识符</td></tr></tbody></table><hr><h2 id="grow-admin-rock-components" tabindex="-1">@grow-admin-rock/components <a class="header-anchor" href="#grow-admin-rock-components" aria-label="Permalink to &quot;@grow-admin-rock/components&quot;">​</a></h2><p>契约组件层，业务代码的主要 UI 入口。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>RockComponent</code></td><td>84 个组件契约枚举</td></tr><tr><td><code>Lib</code></td><td>Library 声明（注册 Grow* 全局组件）</td></tr><tr><td><code>ComponentMap</code></td><td>组件映射表</td></tr><tr><td><code>useDriverComponent</code></td><td>获取驱动组件</td></tr><tr><td><code>useMessage</code> / <code>useNotice</code> / <code>useDialog</code></td><td>命令式 API</td></tr><tr><td><code>setMessage</code> / <code>setNotice</code> / <code>setDialog</code></td><td>驱动绑定注入</td></tr><tr><td><code>withInstall</code></td><td>组件安装工具</td></tr><tr><td><code>ContextParamDef</code></td><td>AppContext 参数标识</td></tr></tbody></table><hr><h2 id="grow-admin-rock-component-driver" tabindex="-1">@grow-admin-rock/component-driver <a class="header-anchor" href="#grow-admin-rock-component-driver" aria-label="Permalink to &quot;@grow-admin-rock/component-driver&quot;">​</a></h2><p>组件驱动桥接基础包。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>ComponentDriverProvider</code></td><td>局部驱动覆盖组件</td></tr><tr><td><code>createDriverHook</code></td><td>驱动 Hook 创建</td></tr><tr><td><code>GrowAdminComponentDriver</code></td><td>驱动接口类型</td></tr><tr><td><code>RockComponent</code></td><td>重导出契约枚举</td></tr></tbody></table><p><strong>三个驱动子包</strong>均实现 <code>GrowAdminComponentDriver</code> 接口，提供 <code>builder().enableXxx().finish()</code> API。</p><hr><h2 id="grow-admin-rock-infrastructure" tabindex="-1">@grow-admin-rock/infrastructure <a class="header-anchor" href="#grow-admin-rock-infrastructure" aria-label="Permalink to &quot;@grow-admin-rock/infrastructure&quot;">​</a></h2><p>HTTP 请求基础设施。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>Lib</code></td><td>Library 声明（注册 Axios 服务）</td></tr><tr><td><code>InfrastructureAxios</code></td><td>Axios 封装类</td></tr><tr><td><code>setPromoter</code></td><td>基础设施配置注入</td></tr><tr><td><code>RequestCanceler</code></td><td>请求取消器</td></tr><tr><td><code>InfrastructureHelper</code></td><td>辅助工具</td></tr><tr><td><code>axios</code></td><td>重导出 axios</td></tr></tbody></table><p><strong>IOC 服务：</strong></p><ul><li><code>InfrastructureAxios</code> — HTTP 客户端</li><li><code>CreateAxiosOptions</code> — Axios 创建配置</li><li><code>AxiosTransform</code> — 请求/响应转换器</li></ul><hr><h2 id="grow-admin-rock-middleware-router" tabindex="-1">@grow-admin-rock/middleware-router <a class="header-anchor" href="#grow-admin-rock-middleware-router" aria-label="Permalink to &quot;@grow-admin-rock/middleware-router&quot;">​</a></h2><p>路由与菜单中间件。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>Lib</code></td><td>Library 声明</td></tr><tr><td><code>RoutesTable</code></td><td>路由表（含 Vue Router 实例）</td></tr><tr><td><code>RouteOperator</code></td><td>路由操作器</td></tr><tr><td><code>MenuState</code></td><td>菜单状态管理</td></tr></tbody></table><p>路由表在 IOC 加载时根据 <code>AppContext.basicRoutes</code> 和 <code>AppContext.appRoutes</code> 动态创建。</p><p>业务代码通过 <code>resolveByKeyOrThrow(routeLib.types.RouteTable).router</code> 获取 router 实例，<strong>布局层与动态路由注册均遵循此约定</strong>。<code>apps-login</code> 的 <code>useLoginSuccess</code> 等少数历史代码仍直接使用 <code>useRouter()</code>，新代码建议统一走 IoC。</p><hr><h2 id="grow-admin-rock-mock" tabindex="-1">@grow-admin-rock/mock <a class="header-anchor" href="#grow-admin-rock-mock" aria-label="Permalink to &quot;@grow-admin-rock/mock&quot;">​</a></h2><p>Mock 注册中心与响应工具。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>Lib</code></td><td>Library 声明（注册 <code>MockRegistry</code>）</td></tr><tr><td><code>registerMock</code></td><td>业务包注册 Mock 方法</td></tr><tr><td><code>getMockModules</code></td><td>聚合所有已注册 Mock</td></tr><tr><td><code>resultSuccess</code> / <code>resultError</code></td><td>统一 <code>{ type, data, message }</code> 响应格式</td></tr></tbody></table><p>详见 <a href="/guide/development/mock">Mock 数据</a>。</p><hr><h2 id="grow-admin-rock-layouts" tabindex="-1">@grow-admin-rock/layouts <a class="header-anchor" href="#grow-admin-rock-layouts" aria-label="Permalink to &quot;@grow-admin-rock/layouts&quot;">​</a></h2><p>布局与项目配置 UI 组件。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>Layout</code> / <code>LayoutLogo</code></td><td>主布局壳与 Logo</td></tr><tr><td><code>SettingDrawer</code></td><td>项目配置抽屉</td></tr><tr><td><code>SettingTheme</code></td><td>主题模式 + 主题色表单项</td></tr><tr><td><code>SwitchLanguage</code></td><td>语言切换表单项</td></tr><tr><td><code>Menu</code></td><td>侧边菜单容器（从 <code>authStore.backMenuList</code> 读取）</td></tr><tr><td><code>PageLoading</code></td><td>页面加载动画</td></tr></tbody></table><p><code>Menu</code> 组件通过 <code>MenuTreeNode</code> 递归渲染树形菜单，使用 <code>GrowSubMenu</code> / <code>GrowMenuItem</code> 契约组件，点击跳转通过 IoC 获取 router。</p><hr><h2 id="grow-admin-rock-settings" tabindex="-1">@grow-admin-rock/settings <a class="header-anchor" href="#grow-admin-rock-settings" aria-label="Permalink to &quot;@grow-admin-rock/settings&quot;">​</a></h2><p>项目设置 IOC 服务（<code>SettingStore</code>、<code>GlobConfig</code>、<code>MenuSettingData</code> 等），与 <code>projectSetting.ts</code> 配合。</p><p><strong>功能：</strong></p><ul><li>主题色切换</li><li>暗色模式</li><li>布局模式（侧边栏 / 混合 / 顶部）</li><li>菜单折叠、宽度</li><li>多标签页配置</li><li>页面过渡动画</li></ul><div class="info custom-block"><p class="custom-block-title">装配状态</p><p>当前 <strong>sample 宿主未默认装配</strong> <code>settingsLib</code>。<code>useGlobConfig</code>（OAuth、authMode）依赖此包 + <code>hooks</code>。详见 <a href="/guide/development/project-setting#可选模块-settings-hooks">项目配置 - 可选模块</a>。</p></div><hr><h2 id="grow-admin-rock-hooks" tabindex="-1">@grow-admin-rock/hooks <a class="header-anchor" href="#grow-admin-rock-hooks" aria-label="Permalink to &quot;@grow-admin-rock/hooks&quot;">​</a></h2><p>通用 Hooks（<code>useGlobConfig</code>、<code>useBreakpoint</code> 等）。</p><p><code>library.ts</code> 的 <code>onSetup</code> 通过 <code>createBreakpointListen</code> 更新移动端状态，<strong>需同时装配 <code>settings</code> + <code>hooks</code> 才完整生效</strong>。</p><hr><h2 id="grow-admin-rock-state" tabindex="-1">@grow-admin-rock/state <a class="header-anchor" href="#grow-admin-rock-state" aria-label="Permalink to &quot;@grow-admin-rock/state&quot;">​</a></h2><p>Pinia 状态管理封装。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>defineStore</code></td><td>重导出 Pinia defineStore</td></tr><tr><td><code>storeToRefs</code></td><td>重导出 storeToRefs</td></tr><tr><td><code>Lib</code></td><td>Library 声明</td></tr><tr><td><code>useAppConfig</code></td><td>项目配置状态（主题色、主题模式等）</td></tr><tr><td><code>useAuthStore</code></td><td>认证状态（<code>backMenuList</code>、<code>isDynamicAddedRoute</code> 等）</td></tr><tr><td><code>useAppStore</code></td><td>页面 loading（<code>setPageLoading</code>、<code>setPageLoadingTip</code>）</td></tr><tr><td><code>useLayout</code></td><td>布局状态（菜单折叠等）</td></tr><tr><td><code>useTheme</code></td><td>主题 CSS 变量动态写入</td></tr><tr><td><code>useLoginRememberStore</code></td><td>登录记住密码（pinia persist）</td></tr><tr><td><code>DataEventBus</code></td><td>mitt 事件总线 + Vue 生命周期 <code>$watchWithVue</code></td></tr></tbody></table><p><code>authStore.backMenuList</code> 存储接口返回的树形菜单数据，供 <code>rock-layouts/Menu</code> 渲染侧边栏。</p><hr><h2 id="grow-admin-rock-locale" tabindex="-1">@grow-admin-rock/locale <a class="header-anchor" href="#grow-admin-rock-locale" aria-label="Permalink to &quot;@grow-admin-rock/locale&quot;">​</a></h2><p>国际化支持。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>useI18n</code></td><td>i18n Hook</td></tr><tr><td><code>t</code> / <code>$t</code></td><td>翻译函数</td></tr><tr><td><code>useLocale</code></td><td>语言切换 Hook</td></tr><tr><td><code>localeHelper</code></td><td>国际化辅助工具</td></tr></tbody></table><hr><h2 id="grow-admin-rock-hooks-1" tabindex="-1">@grow-admin-rock/hooks <a class="header-anchor" href="#grow-admin-rock-hooks-1" aria-label="Permalink to &quot;@grow-admin-rock/hooks&quot;">​</a></h2><p>通用 Hooks 集合。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>SettingHooks</code></td><td>项目设置相关 Hooks</td></tr><tr><td><code>useBreakpoint</code></td><td>响应式断点 Hook</td></tr></tbody></table><hr><h2 id="grow-admin-rock-utils" tabindex="-1">@grow-admin-rock/utils <a class="header-anchor" href="#grow-admin-rock-utils" aria-label="Permalink to &quot;@grow-admin-rock/utils&quot;">​</a></h2><p>工具函数库，同时重导出 <code>@vueuse/core</code>。</p><p><strong>主要模块：</strong></p><table><thead><tr><th>模块</th><th>说明</th></tr></thead><tbody><tr><td><code>config</code></td><td>配置工具</td></tr><tr><td><code>is</code></td><td>类型判断</td></tr><tr><td><code>toDataType</code></td><td>数据类型转换</td></tr><tr><td><code>utils</code></td><td>通用工具（clone、deepMerge 等）</td></tr></tbody></table><hr><h2 id="grow-admin-rock-constants" tabindex="-1">@grow-admin-rock/constants <a class="header-anchor" href="#grow-admin-rock-constants" aria-label="Permalink to &quot;@grow-admin-rock/constants&quot;">​</a></h2><p>框架枚举与常量。</p><p><strong>主要枚举：</strong></p><table><thead><tr><th>枚举</th><th>说明</th></tr></thead><tbody><tr><td><code>ComponentLibraryType</code></td><td>组件库类型</td></tr><tr><td><code>ThemeEnum</code></td><td>主题（明/暗）</td></tr><tr><td><code>MenuModeEnum</code> / <code>MenuTypeEnum</code></td><td>菜单模式与类型</td></tr><tr><td><code>PermissionModeEnum</code></td><td>权限模式</td></tr><tr><td><code>ContentLayoutEnum</code></td><td>内容布局</td></tr><tr><td><code>RouterTransitionEnum</code></td><td>路由过渡动画</td></tr><tr><td><code>CacheTypeEnum</code></td><td>缓存类型</td></tr></tbody></table><hr><h2 id="grow-admin-rock-types" tabindex="-1">@grow-admin-rock/types <a class="header-anchor" href="#grow-admin-rock-types" aria-label="Permalink to &quot;@grow-admin-rock/types&quot;">​</a></h2><p>全局 TypeScript 类型定义，被所有 Rock 包引用。</p><p><strong>主要类型：</strong></p><ul><li><code>ProjectSetting</code> — 项目配置</li><li><code>RouteRecordItem</code> — 路由记录</li><li><code>RequestOptions</code> — 请求配置</li><li><code>WmqComponentDictionary</code> — 组件驱动字典</li></ul><hr><h2 id="grow-admin-rock-styles" tabindex="-1">@grow-admin-rock/styles <a class="header-anchor" href="#grow-admin-rock-styles" aria-label="Permalink to &quot;@grow-admin-rock/styles&quot;">​</a></h2><p>全局样式入口，在 <code>sample/src/main.ts</code> 中首行引入：</p><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">import</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span><span style="${ssrRenderStyle({ "color": "#C3E88D" })}">@grow-admin-rock/styles</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">&#39;</span></span></code></pre></div><p>包含 Reset CSS、CSS 变量（<code>variables.css</code>）、亮/暗主题 token 与主题切换过渡动画（<code>theme-transition.css</code>）。</p><hr><h2 id="grow-admin-rock-code-sandbox" tabindex="-1">@grow-admin-rock/code-sandbox <a class="header-anchor" href="#grow-admin-rock-code-sandbox" aria-label="Permalink to &quot;@grow-admin-rock/code-sandbox&quot;">​</a></h2><p>在线代码编辑与 Vue SFC 沙箱预览（Monaco + <code>@vue/compiler-sfc</code>）。预览挂载在宿主 Vue 树内，可继续使用 <code>Grow*</code> 组件与 IOC。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>GrowCodeEditor</code></td><td>Monaco 代码编辑器</td></tr><tr><td><code>GrowCodeDeps</code></td><td>依赖注入面板（host / npm）</td></tr><tr><td><code>GrowCodeSandbox</code></td><td>SFC 编译预览</td></tr><tr><td><code>composeVueSfc</code> / <code>parseVueSfc</code></td><td>组装 / 解析 SFC</td></tr><tr><td><code>createPreviewComponent</code></td><td>编译 SFC 为可挂载组件</td></tr><tr><td><code>DEFAULT_SANDBOX_DEPENDENCIES</code></td><td>默认锁定依赖</td></tr><tr><td><code>mergeDependencies</code> / <code>normalizeDependencies</code></td><td>依赖合并规范化</td></tr></tbody></table><p>目录：<code>DesignRock/rock-code-sandbox</code>。</p><p>完整 API、用法与注意事项见 <a href="/code-sandbox/">代码沙箱</a>。演示模块见 <a href="/guide/packages/design-cornerstone#grow-admin-cornerstoneapps-sandbox">apps-sandbox</a>。</p><hr><h2 id="grow-admin-rock-designer" tabindex="-1">@grow-admin-rock/designer <a class="header-anchor" href="#grow-admin-rock-designer" aria-label="Permalink to &quot;@grow-admin-rock/designer&quot;">​</a></h2><p>低代码页面设计器：拖拽物料、配置属性 / 样式 / 事件，支持数据源与变量绑定，输出可渲染 schema。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>GrowDesigner</code></td><td>可视化编辑器（画布 + 左右面板）</td></tr><tr><td><code>GrowRenderer</code></td><td>按 <code>:schema</code> 渲染页面（含绑定求值）</td></tr><tr><td><code>DesignerDataSourcePanel</code> 等</td><td>页面级数据面板（报表设计器可复用）</td></tr><tr><td><code>GROW_RUNTIME_STATE</code> / <code>runApiOutlinedList</code> 等</td><td>运行时 state 与 API 执行</td></tr></tbody></table><p>目录：<code>DesignRock/rock-designer</code>。</p><p>完整说明见 <a href="/page-designer/">页面设计器</a>。演示模块见 <a href="/guide/packages/design-cornerstone#grow-admin-cornerstoneapps-designer">apps-designer</a>。</p><hr><h2 id="grow-admin-rock-report-designer" tabindex="-1">@grow-admin-rock/report-designer <a class="header-anchor" href="#grow-admin-rock-report-designer" aria-label="Permalink to &quot;@grow-admin-rock/report-designer&quot;">​</a></h2><p>基于 ECharts 的报表设计器：网格布局、图表视觉配置、页面级数据 + 区块数据绑定，导出 <code>ReportSchema</code> 并由渲染器拉数展示。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>GrowReportDesigner</code></td><td>可视化编辑器（<code>getSchema</code> / <code>runtimeState</code>）</td></tr><tr><td><code>GrowReportRenderer</code></td><td>按 <code>:schema</code> 渲染报表（可选 <code>httpClient</code>）</td></tr><tr><td><code>createReportSchema</code></td><td>组装带默认 pageConfig 的 schema</td></tr><tr><td><code>buildEChartsOption</code> / <code>createDefaultChartConfig</code></td><td>图表 option 编译与默认配置</td></tr><tr><td><code>REPORT_CHART_TYPE_OPTIONS</code> 等</td><td>图表类型常量</td></tr></tbody></table><p>目录：<code>DesignRock/rock-report-designer</code>。</p><p>完整说明见 <a href="/report-designer/">报表设计器</a>。演示入口同 <a href="/guide/packages/design-cornerstone#grow-admin-cornerstoneapps-designer">apps-designer</a>（菜单：<strong>报表设计器</strong>）。区块数据经页面 <code>state</code> 绑定；可与 <a href="/data-prep/">数据准备</a> 查询结果对接（由数据请求写入 state）。工具链见 <a href="/guide/designers/">低代码设计器</a>。</p><hr><h2 id="grow-admin-rock-schema-designer" tabindex="-1">@grow-admin-rock/schema-designer <a class="header-anchor" href="#grow-admin-rock-schema-designer" aria-label="Permalink to &quot;@grow-admin-rock/schema-designer&quot;">​</a></h2><p>基于 Vue Flow 的可视化数据库建模：表 / 字段 / 关联（1:1、1:N、N:N），导出可持久化的 <code>DatabaseSchema</code> JSON。当前方言为 PostgreSQL。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>GrowSchemaDesigner</code></td><td>可视化编辑器（<code>v-model</code> / <code>getSchema</code> / <code>setSchema</code> / <code>exportJson</code>）</td></tr><tr><td><code>createDatabaseSchema</code> / <code>createSchemaTable</code> / <code>createSchemaColumn</code></td><td>schema 工厂</td></tr><tr><td><code>exportSchemaJson</code></td><td>序列化为可持久化 JSON 字符串</td></tr><tr><td><code>SCHEMA_COLUMN_TYPE_OPTIONS</code> / <code>RELATION_TYPE_OPTIONS</code></td><td>PostgreSQL 字段类型与关联类型选项</td></tr><tr><td><code>clampIdentifier</code> / <code>MAX_*_NAME_LENGTH</code></td><td>标识符截断与长度常量（最长 63）</td></tr></tbody></table><p>目录：<code>DesignRock/rock-schema-designer</code>。</p><p>完整说明见 <a href="/schema-designer/">数据库建模</a>。演示入口同 <a href="/guide/packages/design-cornerstone#grow-admin-cornerstoneapps-designer">apps-designer</a>（菜单：<strong>数据库建模</strong>）。</p><hr><h2 id="grow-admin-rock-data-prep" tabindex="-1">@grow-admin-rock/data-prep <a class="header-anchor" href="#grow-admin-rock-data-prep" aria-label="Permalink to &quot;@grow-admin-rock/data-prep&quot;">​</a></h2><p>可视化数据准备（分析层 Dataset）：从 Schema 选表、配置多表 Join、公式度量（<code>metricConfigs</code>）与输出字段投影，支持预览聚合与本地 / Mock 查询。查询结果经页面 / 报表的 <code>apiOutlined</code> 写入 <code>state</code> 后绑定展示；报表不直选 Dataset。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>GrowDataPrepDesigner</code></td><td>可视化编辑器（<code>v-model</code> / <code>@save</code>）</td></tr><tr><td><code>createDataPrepDataset</code> / <code>createDataPrepMetricConfig</code> / <code>createDataPrepJoin</code> 等</td><td>Dataset 工厂</td></tr><tr><td><code>queryDatasetLocal</code> / <code>queryDataPrepDataset</code> / <code>previewMetricConfig</code></td><td>本地聚合 / Mock 查询 / 单配置试算</td></tr><tr><td><code>measureOutputKey</code> / <code>toCartesianSeriesPayload</code></td><td>结果字段名 / 笛卡尔图轴与系列适配</td></tr><tr><td><code>FORMULA_FUNCTION_DOCS</code> / <code>evaluateFormulaOnGroup</code></td><td>公式函数文档与求值</td></tr><tr><td><code>listOutputFieldCandidates</code> / <code>pruneOutputFields</code></td><td>输出字段候选与修剪</td></tr><tr><td><code>loadDatasetsFromStorage</code> / <code>upsertDatasetInStorage</code></td><td>localStorage 读写</td></tr><tr><td><code>ensureDemoDataset</code></td><td>写入演示用 Dataset</td></tr><tr><td><code>./core</code></td><td>无 Vue 的纯 TS 入口（供 Mock esbuild）</td></tr></tbody></table><p>目录：<code>DesignRock/rock-data-prep</code>。</p><p>完整说明见 <a href="/data-prep/">数据准备</a>。工具链见 <a href="/guide/designers/">低代码设计器</a>。演示入口同 <a href="/guide/packages/design-cornerstone#grow-admin-cornerstoneapps-designer">apps-designer</a>（菜单：<strong>数据准备</strong>）。</p><hr><h2 id="grow-admin-rock-data-clean" tabindex="-1">@grow-admin-rock/data-clean <a class="header-anchor" href="#grow-admin-rock-data-clean" aria-label="Permalink to &quot;@grow-admin-rock/data-clean&quot;">​</a></h2><p>可视化数据清洗（声明式 ETL 流）：从组件库拖入源 / 清洗 / 合并 / 聚合 / 输出节点，连成有向无环图，产物为 <code>CleanFlow</code>（设计期编排，<strong>调用时执行</strong>）。提供全量节点配置 UI、本地管道预览（<code>runCleanFlowLocal</code>）与 Mock <code>/data-clean/*</code>；展示侧消费者绑定仍待对接。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>GrowDataCleanDesigner</code></td><td>可视化编辑器（<code>v-model</code> / <code>@change</code> / <code>@save</code>；<code>getFlow</code> / <code>setFlow</code>）</td></tr><tr><td><code>createCleanFlow</code> / <code>createCleanFlowNode</code> / <code>createCleanFlowEdge</code></td><td>流 / 节点 / 边工厂</td></tr><tr><td><code>cloneCleanFlow</code> / <code>defaultConfigForType</code></td><td>深拷贝 / 按类型默认 config</td></tr><tr><td><code>CATEGORY_META</code> / <code>NODE_TYPE_META</code> / <code>PALETTE_GROUPS</code></td><td>类别与组件库元数据</td></tr><tr><td><code>TABLE_SOURCE_KIND_OPTIONS</code> / <code>DEMO_SOURCE_OPTIONS</code></td><td>表源种类与 demo 选项</td></tr><tr><td><code>FILTER_OP_OPTIONS</code> / <code>FILTER_LOGIC_OPTIONS</code> / <code>SPLIT_MODE_OPTIONS</code></td><td>过滤与拆分 UI 选项</td></tr><tr><td><code>runCleanFlowLocal</code> / <code>countOutputNodes</code> / <code>CLEAN_IMPLEMENTED_NODE_TYPES</code></td><td>本地管道执行与输出节点计数</td></tr><tr><td><code>loadCleanTableRowsMap</code> / <code>fetchCleanDemoTables</code> / <code>fetchCleanTableRows</code></td><td>Mock / Demo 表加载</td></tr><tr><td><code>buildDemoPreview</code></td><td>旧版占位采样（兼容导出；设计器主路径已不用）</td></tr></tbody></table><p>目录：<code>DesignRock/rock-data-clean</code>。Mock：<code>sample/mock/dataClean.ts</code>。</p><p>完整说明见 <a href="/data-clean/">数据清洗</a>。工具链见 <a href="/guide/designers/">低代码设计器</a>。演示入口同 <a href="/guide/packages/design-cornerstone#grow-admin-cornerstoneapps-designer">apps-designer</a>（菜单：<strong>数据清洗</strong>）。</p><hr><h2 id="grow-admin-rock-process-engine" tabindex="-1">@grow-admin-rock/process-engine <a class="header-anchor" href="#grow-admin-rock-process-engine" aria-label="Permalink to &quot;@grow-admin-rock/process-engine&quot;">​</a></h2><p>可视化流程编排（业务审批 / 事件 / 系统 / 状态机 / 决策）：从组件库拖入节点，自上而下连成 <code>ProcessFlow</code>。当前为设计期编排与全量配置 UI；尚无运行时执行引擎与独立 Mock。</p><p><strong>主要导出：</strong></p><table><thead><tr><th>导出</th><th>说明</th></tr></thead><tbody><tr><td><code>GrowProcessDesigner</code></td><td>可视化编辑器（<code>v-model</code> / <code>@change</code> / <code>@save</code>；<code>getFlow</code> / <code>setFlow</code>）</td></tr><tr><td><code>createProcessFlow</code> / <code>createProcessFlowNode</code> / <code>createProcessFlowEdge</code></td><td>流 / 节点 / 边工厂</td></tr><tr><td><code>cloneProcessFlow</code> / <code>defaultConfigForType</code></td><td>深拷贝 / 按类型默认 config</td></tr><tr><td><code>CATEGORY_META</code> / <code>NODE_TYPE_META</code> / <code>PALETTE_GROUPS</code></td><td>类别与组件库元数据</td></tr><tr><td><code>PERSON_ASSIGNEE_TYPE_OPTIONS</code> 及各类 UI 选项常量</td><td>指派、超时、驳回、协议等</td></tr><tr><td><code>canConnectNodes</code> / <code>isStartNodeType</code> / <code>isEndNodeType</code></td><td>连线与起止判定</td></tr><tr><td><code>resolveSourceHandle</code> / <code>resolveTargetHandle</code></td><td>Handle 归一化</td></tr></tbody></table><p>目录：<code>DesignRock/rock-process-engine</code>。</p><p>完整说明见 <a href="/process-engine/">流程引擎</a>。演示入口同 <a href="/guide/packages/design-cornerstone#grow-admin-cornerstoneapps-designer">apps-designer</a>（菜单：<strong>流程引擎</strong>）。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/packages/design-rock.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const designRock = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  designRock as default
};
