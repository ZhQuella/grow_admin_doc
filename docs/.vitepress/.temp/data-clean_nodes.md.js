import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.cc2b3d55.js";
const __pageData = JSON.parse('{"title":"数据清洗 · 节点与算子","description":"","frontmatter":{"title":"数据清洗 · 节点与算子","lang":"zh-CN"},"headers":[],"relativePath":"data-clean/nodes.md"}');
const _sfc_main = { name: "data-clean/nodes.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="节点与算子" tabindex="-1">节点与算子 <a class="header-anchor" href="#节点与算子" aria-label="Permalink to &quot;节点与算子&quot;">​</a></h1><p>本文按 <strong>类别</strong> 说明组件库中的全部节点：中文名、端口数、配置字段、与相近节点的差异。类型定义见 <a href="/data-clean/schema">数据模型</a>。十五种节点均已提供配置 UI，并由 <code>runCleanFlowLocal</code> 实现本地变换。</p><h2 id="类别与配色" tabindex="-1">类别与配色 <a class="header-anchor" href="#类别与配色" aria-label="Permalink to &quot;类别与配色&quot;">​</a></h2><table><thead><tr><th>类别</th><th><code>category</code></th><th>中文</th><th>CSS 变量（节点强调色）</th></tr></thead><tbody><tr><td>数据源</td><td><code>source</code></td><td>数据源</td><td><code>--clean-cat-source</code>（青）</td></tr><tr><td>清洗</td><td><code>clean</code></td><td>清洗</td><td><code>--clean-cat-clean</code>（琥珀）</td></tr><tr><td>合并</td><td><code>merge</code></td><td>合并</td><td><code>--clean-cat-merge</code>（紫）</td></tr><tr><td>聚合</td><td><code>agg</code></td><td>聚合</td><td><code>--clean-cat-agg</code>（靛）</td></tr><tr><td>输出</td><td><code>output</code></td><td>输出</td><td><code>--clean-cat-output</code>（绿）</td></tr></tbody></table><h2 id="端口与连线" tabindex="-1">端口与连线 <a class="header-anchor" href="#端口与连线" aria-label="Permalink to &quot;端口与连线&quot;">​</a></h2><p>每个类型在 <code>NODE_TYPE_META</code> 中声明 <code>inputs</code> / <code>outputs</code>：</p><table><thead><tr><th>规则</th><th>说明</th></tr></thead><tbody><tr><td>输入在左、输出在右</td><td>禁止从 in 拖到 out（方向校验）</td></tr><tr><td><code>inputs === 0</code></td><td>源节点，无左侧 Handle</td></tr><tr><td><code>outputs === 0</code></td><td>输出节点，无右侧 Handle</td></tr><tr><td><code>inputs &gt;= 2</code></td><td>左侧额外 <code>in-left-top</code> / <code>in-left-bottom</code>（Join / Union）</td></tr><tr><td><code>outputs === 2</code></td><td>右侧 <code>out-true</code>（是）/ <code>out-false</code>（否），仅条件分支</td></tr><tr><td>普通单出</td><td><code>out-right</code>，并附带上下辅助出点便于一对多分发</td></tr></tbody></table><p>常用 Handle id：</p><table><thead><tr><th>Handle</th><th>含义</th></tr></thead><tbody><tr><td><code>in-left</code></td><td>主输入</td></tr><tr><td><code>in-left-top</code> / <code>in-left-bottom</code></td><td>多输入辅助</td></tr><tr><td><code>out-right</code> / <code>out-right-top</code> / <code>out-right-bottom</code></td><td>普通输出</td></tr><tr><td><code>out-true</code> / <code>out-false</code></td><td>条件分支「是 / 否」</td></tr></tbody></table><p>画布还禁止：自连、成环、同一 <code>source+target+sourceHandle</code> 重复边；<strong>整图仅允许一个 <code>output</code> 节点</strong>。</p><hr><h2 id="数据源-source" tabindex="-1">数据源（source） <a class="header-anchor" href="#数据源-source" aria-label="Permalink to &quot;数据源（source）&quot;">​</a></h2><h3 id="数据表-table" tabindex="-1">数据表 <code>table</code> <a class="header-anchor" href="#数据表-table" aria-label="Permalink to &quot;数据表 \`table\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>入 0 / 出 1</td></tr><tr><td>说明</td><td>从建模或数据准备选择表 / 输出</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong>（含字段勾选）</td></tr></tbody></table><table><thead><tr><th>配置字段</th><th>说明</th></tr></thead><tbody><tr><td><code>sourceKind</code></td><td><code>schema-table</code> | <code>dataset-table</code> | <code>dataset-output</code></td></tr><tr><td><code>refId</code> / <code>refLabel</code></td><td>选中项 id 与展示名</td></tr><tr><td><code>tableId</code> / <code>tableName</code></td><td>表标识</td></tr><tr><td><code>fields</code></td><td><code>undefined</code>/<code>null</code> = 全部；非空数组 = 投影；<code>[]</code> = 无字段</td></tr></tbody></table><p>Demo 表示例（<code>DEMO_SOURCE_OPTIONS</code>）：</p><ul><li>建模：<code>demo_sales · orders / customers</code>，<code>demo_region · regions</code></li><li>Dataset 原始表：<code>销售数据集 · orders / customers</code></li><li>Dataset 输出：<code>销售数据集 · 查询输出</code></li></ul><h3 id="api-接口-api" tabindex="-1">API 接口 <code>api</code> <a class="header-anchor" href="#api-接口-api" aria-label="Permalink to &quot;API 接口 \`api\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>入 0 / 出 1</td></tr><tr><td>说明</td><td>从 HTTP 接口拉取数据</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>配置字段</th><th>默认</th></tr></thead><tbody><tr><td><code>url</code></td><td><code>&#39;&#39;</code>（可填 <code>/demo/orders</code> 等；留空用包内默认样例帧）</td></tr><tr><td><code>method</code></td><td><code>&#39;GET&#39;</code> | <code>&#39;POST&#39;</code></td></tr></tbody></table><p>本地预览通过 <code>resolveDemoApiFrame</code> 解析 Demo 帧，不发起真实跨域请求。</p><hr><h2 id="清洗-clean" tabindex="-1">清洗（clean） <a class="header-anchor" href="#清洗-clean" aria-label="Permalink to &quot;清洗（clean）&quot;">​</a></h2><h3 id="空值处理-null-handle" tabindex="-1">空值处理 <code>null-handle</code> <a class="header-anchor" href="#空值处理-null-handle" aria-label="Permalink to &quot;空值处理 \`null-handle\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td>填充 / 删除行 / 前后向填充</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>取值</th></tr></thead><tbody><tr><td><code>fields</code></td><td>作用列</td></tr><tr><td><code>strategy</code></td><td><code>fill</code> | <code>drop-row</code> | <code>ffill</code> | <code>bfill</code></td></tr><tr><td><code>fillValue</code></td><td><code>strategy=fill</code> 时的填充值</td></tr></tbody></table><h3 id="格式标准化-format" tabindex="-1">格式标准化 <code>format</code> <a class="header-anchor" href="#格式标准化-format" aria-label="Permalink to &quot;格式标准化 \`format\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td>手机号 / 日期 / 金额等格式</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>取值</th></tr></thead><tbody><tr><td><code>field</code></td><td>目标列</td></tr><tr><td><code>format</code></td><td><code>phone</code> | <code>id-card</code> | <code>date</code> | <code>money</code> | <code>regex</code></td></tr><tr><td><code>pattern</code></td><td><code>regex</code> 时的模式（含捕获组时取第 1 组）</td></tr></tbody></table><h3 id="去重-dedupe" tabindex="-1">去重 <code>dedupe</code> <a class="header-anchor" href="#去重-dedupe" aria-label="Permalink to &quot;去重 \`dedupe\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td>按字段组合去重</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>取值</th></tr></thead><tbody><tr><td><code>fields</code></td><td>判重键</td></tr><tr><td><code>keep</code></td><td><code>first</code> | <code>last</code> | <code>random</code></td></tr></tbody></table><h3 id="去空格-大小写-trim-case" tabindex="-1">去空格&amp;大小写 <code>trim-case</code> <a class="header-anchor" href="#去空格-大小写-trim-case" aria-label="Permalink to &quot;去空格&amp;大小写 \`trim-case\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td>空格与大小写规范化</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>取值</th></tr></thead><tbody><tr><td><code>fields</code></td><td>作用列</td></tr><tr><td><code>ops</code></td><td><code>trim</code> | <code>trim-all</code> | <code>upper</code> | <code>lower</code> | <code>capitalize</code>（可多选）</td></tr></tbody></table><h3 id="异常值处理-outlier" tabindex="-1">异常值处理 <code>outlier</code> <a class="header-anchor" href="#异常值处理-outlier" aria-label="Permalink to &quot;异常值处理 \`outlier\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td>范围 / 正则 / 枚举校验</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>取值</th></tr></thead><tbody><tr><td><code>field</code></td><td>目标列</td></tr><tr><td><code>rule</code></td><td><code>range</code> | <code>regex</code> | <code>enum</code></td></tr><tr><td><code>action</code></td><td><code>mark</code> | <code>drop</code> | <code>replace</code></td></tr><tr><td><code>replaceValue</code></td><td><code>action=replace</code> 时替换值</td></tr><tr><td><code>min</code> / <code>max</code></td><td><code>rule=range</code></td></tr><tr><td><code>pattern</code></td><td><code>rule=regex</code></td></tr><tr><td><code>enumValues</code></td><td><code>rule=enum</code>，逗号分隔合法值</td></tr></tbody></table><h3 id="条件过滤-filter" tabindex="-1">条件过滤 <code>filter</code> <a class="header-anchor" href="#条件过滤-filter" aria-label="Permalink to &quot;条件过滤 \`filter\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td><strong>丢弃</strong>不满足条件的行</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>logic</code></td><td>多条件组合：<code>and</code> | <code>or</code>（条件数 &gt; 1 时显示）</td></tr><tr><td><code>conditions[]</code></td><td><code>{ field, op, value }</code></td></tr></tbody></table><p>界面提示：仅保留满足条件的行，不满足的行会被丢弃。</p><h3 id="条件分支-condition" tabindex="-1">条件分支 <code>condition</code> <a class="header-anchor" href="#条件分支-condition" aria-label="Permalink to &quot;条件分支 \`condition\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / <strong>2</strong></td></tr><tr><td>说明</td><td><strong>保留全部行</strong>，按条件分流到「是 / 否」两路下游</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong>（表单与 filter 相同，语义不同）</td></tr></tbody></table><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>logic</code> / <code>conditions</code></td><td>同 <code>CleanFilterConfig</code></td></tr></tbody></table><p>界面提示：满足条件从右侧「是」出口流出，否则从「否」出口流出；两路均可继续连接下游。</p><div class="tip custom-block"><p class="custom-block-title">filter vs condition</p><ul><li><strong>过滤</strong>：行集合变小（不满足 → 删除）。</li><li><strong>分支</strong>：行集合按条件拆到两条管道，总量不变（分别进入不同下游）。</li></ul></div><h3 id="字段拆分-split-field" tabindex="-1">字段拆分 <code>split-field</code> <a class="header-anchor" href="#字段拆分-split-field" aria-label="Permalink to &quot;字段拆分 \`split-field\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td>将一个字段拆分为多个字段</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong>；预览随配置变化</td></tr></tbody></table><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>field</code></td><td>源字段名</td></tr><tr><td><code>mode</code></td><td><code>delimiter</code> | <code>regex</code> | <code>fixed-width</code></td></tr><tr><td><code>delimiter</code></td><td>分隔符模式，如 <code>,</code> / <code>|</code> / 空格</td></tr><tr><td><code>pattern</code></td><td>正则捕获组，如 <code>^(\\\\d{4})-(\\\\d{2})-(\\\\d{2})$</code></td></tr><tr><td><code>outputs</code></td><td>输出字段列表；固定宽度可带 <code>width</code></td></tr><tr><td><code>keepOriginal</code></td><td>是否保留原字段，默认 <code>true</code></td></tr><tr><td><code>padEmpty</code></td><td>段数不足时是否用空值补齐，默认 <code>true</code></td></tr></tbody></table><p>示例：</p><ul><li><code>张三,李四</code> + 分隔符 <code>,</code> → <code>first_name</code> / <code>last_name</code></li><li>日期 <code>2026-08-10</code> + 正则捕获 → 年 / 月 / 日三列</li></ul><hr><h2 id="过滤运算符" tabindex="-1">过滤运算符 <a class="header-anchor" href="#过滤运算符" aria-label="Permalink to &quot;过滤运算符&quot;">​</a></h2><p><code>FILTER_OP_OPTIONS</code>（filter / condition 共用）：</p><table><thead><tr><th>标签</th><th><code>op</code></th><th>是否需要 <code>value</code></th></tr></thead><tbody><tr><td>等于</td><td><code>eq</code></td><td>是</td></tr><tr><td>不等于</td><td><code>neq</code></td><td>是</td></tr><tr><td>大于</td><td><code>gt</code></td><td>是</td></tr><tr><td>大于等于</td><td><code>gte</code></td><td>是</td></tr><tr><td>小于</td><td><code>lt</code></td><td>是</td></tr><tr><td>小于等于</td><td><code>lte</code></td><td>是</td></tr><tr><td>包含</td><td><code>contains</code></td><td>是</td></tr><tr><td>不包含</td><td><code>not-contains</code></td><td>是</td></tr><tr><td>为空</td><td><code>empty</code></td><td>否（界面隐藏值输入）</td></tr><tr><td>不为空</td><td><code>not-empty</code></td><td>否</td></tr></tbody></table><p>组合逻辑：<code>且 (AND)</code> / <code>或 (OR)</code>。</p><hr><h2 id="合并-merge" tabindex="-1">合并（merge） <a class="header-anchor" href="#合并-merge" aria-label="Permalink to &quot;合并（merge）&quot;">​</a></h2><h3 id="关联合并-join" tabindex="-1">关联合并 <code>join</code> <a class="header-anchor" href="#关联合并-join" aria-label="Permalink to &quot;关联合并 \`join\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td><strong>2</strong> / 1</td></tr><tr><td>说明</td><td>LEFT / INNER / RIGHT / FULL JOIN</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>joinType</code></td><td><code>left</code> | <code>inner</code> | <code>right</code> | <code>full</code>（默认 <code>left</code>）</td></tr><tr><td><code>keys</code></td><td><code>{ leftField, rightField }[]</code></td></tr><tr><td><code>outputFields</code></td><td>输出列投影（可选）</td></tr></tbody></table><p>连线时请把两路上游分别接到左侧不同输入锚点。</p><h3 id="纵向合并-union" tabindex="-1">纵向合并 <code>union</code> <a class="header-anchor" href="#纵向合并-union" aria-label="Permalink to &quot;纵向合并 \`union\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td><strong>2</strong> / 1</td></tr><tr><td>说明</td><td>UNION 多路输入</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>dedupe</code></td><td>是否去重，默认 <code>false</code></td></tr><tr><td><code>fieldMap</code></td><td>字段名映射；界面可用 <code>右字段:左字段</code> 逗号列表编辑</td></tr></tbody></table><hr><h2 id="聚合-agg" tabindex="-1">聚合（agg） <a class="header-anchor" href="#聚合-agg" aria-label="Permalink to &quot;聚合（agg）&quot;">​</a></h2><h3 id="分组聚合-groupby" tabindex="-1">分组聚合 <code>groupby</code> <a class="header-anchor" href="#分组聚合-groupby" aria-label="Permalink to &quot;分组聚合 \`groupby\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td>GROUP BY + 聚合度量</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>groupFields</code></td><td>分组列</td></tr><tr><td><code>metrics</code></td><td><code>{ field, fn, alias }[]</code>，<code>fn</code> 为 <code>SUM</code> | <code>COUNT</code> | <code>AVG</code> | <code>MAX</code> | <code>MIN</code></td></tr></tbody></table><p>与 <a href="/data-prep/formulas">数据准备公式度量</a> 不同：此处是节点级结构化聚合配置，不是自由公式文本。</p><h3 id="透视表-pivot" tabindex="-1">透视表 <code>pivot</code> <a class="header-anchor" href="#透视表-pivot" aria-label="Permalink to &quot;透视表 \`pivot\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / 1</td></tr><tr><td>说明</td><td>行 / 列维度透视</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong></td></tr></tbody></table><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>rowField</code> / <code>colField</code> / <code>valueField</code></td><td>行维、列维、值列</td></tr><tr><td><code>agg</code></td><td><code>SUM</code> | <code>COUNT</code> | <code>AVG</code> | <code>MAX</code> | <code>MIN</code>（默认 <code>SUM</code>）</td></tr></tbody></table><hr><h2 id="输出-output" tabindex="-1">输出（output） <a class="header-anchor" href="#输出-output" aria-label="Permalink to &quot;输出（output）&quot;">​</a></h2><h3 id="数据输出-output" tabindex="-1">数据输出 <code>output</code> <a class="header-anchor" href="#数据输出-output" aria-label="Permalink to &quot;数据输出 \`output\`&quot;">​</a></h3><table><thead><tr><th>项</th><th>值</th></tr></thead><tbody><tr><td>端口</td><td>1 / <strong>0</strong></td></tr><tr><td>说明</td><td>流终点，供报表 / 页面调用时执行</td></tr><tr><td>配置 UI</td><td><strong>已实现</strong>（名称 + 目标 + 输出字段勾选）</td></tr></tbody></table><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>outputName</code></td><td>输出名，默认「清洗输出」</td></tr><tr><td><code>target</code></td><td><code>report</code>（报表数据集）| <code>lowcode</code>（低代码页面数据源）| <code>api</code>（API 端点）</td></tr><tr><td><code>trigger</code></td><td><code>on-demand</code> | <code>manual-preview</code>（预留）</td></tr><tr><td><code>consumers</code></td><td><code>{ id, name, kind: &#39;report&#39;|&#39;page&#39; }[]</code>（预留）</td></tr><tr><td><code>fields</code></td><td>最终输出投影；未配置默认全部，也可全部取消（<code>[]</code>）</td></tr></tbody></table><p>界面提示：调用时执行；消费者绑定后续对接。画布上<strong>只能有一个</strong>输出节点。</p><hr><h2 id="节点速查表" tabindex="-1">节点速查表 <a class="header-anchor" href="#节点速查表" aria-label="Permalink to &quot;节点速查表&quot;">​</a></h2><table><thead><tr><th>type</th><th>中文</th><th>类别</th><th>入/出</th><th>配置 UI</th><th>本地变换</th></tr></thead><tbody><tr><td><code>table</code></td><td>数据表</td><td>source</td><td>0/1</td><td>✅</td><td>✅</td></tr><tr><td><code>api</code></td><td>API 接口</td><td>source</td><td>0/1</td><td>✅</td><td>✅（Demo 帧）</td></tr><tr><td><code>null-handle</code></td><td>空值处理</td><td>clean</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>format</code></td><td>格式标准化</td><td>clean</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>dedupe</code></td><td>去重</td><td>clean</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>trim-case</code></td><td>去空格&amp;大小写</td><td>clean</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>outlier</code></td><td>异常值处理</td><td>clean</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>filter</code></td><td>条件过滤</td><td>clean</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>condition</code></td><td>条件分支</td><td>clean</td><td>1/2</td><td>✅</td><td>✅</td></tr><tr><td><code>split-field</code></td><td>字段拆分</td><td>clean</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>join</code></td><td>关联合并</td><td>merge</td><td>2/1</td><td>✅</td><td>✅</td></tr><tr><td><code>union</code></td><td>纵向合并</td><td>merge</td><td>2/1</td><td>✅</td><td>✅</td></tr><tr><td><code>groupby</code></td><td>分组聚合</td><td>agg</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>pivot</code></td><td>透视表</td><td>agg</td><td>1/1</td><td>✅</td><td>✅</td></tr><tr><td><code>output</code></td><td>数据输出</td><td>output</td><td>1/0</td><td>✅</td><td>✅</td></tr></tbody></table><h2 id="相关文档" tabindex="-1">相关文档 <a class="header-anchor" href="#相关文档" aria-label="Permalink to &quot;相关文档&quot;">​</a></h2><ul><li><a href="/data-clean/">概述</a></li><li><a href="/data-clean/usage">基础用法</a></li><li><a href="/data-clean/schema">数据模型</a></li><li><a href="/data-prep/joins">数据准备 · 表关联</a> — 分析层 Join，与清洗 <code>join</code> 节点不同场景</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("data-clean/nodes.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const nodes = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  nodes as default
};
