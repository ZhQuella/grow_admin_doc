import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.cc2b3d55.js";
const __pageData = JSON.parse('{"title":"认证与登录","description":"","frontmatter":{"title":"认证与登录","lang":"zh-CN"},"headers":[],"relativePath":"guide/development/authentication.md"}');
const _sfc_main = { name: "guide/development/authentication.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="认证与登录" tabindex="-1">认证与登录 <a class="header-anchor" href="#认证与登录" aria-label="Permalink to &quot;认证与登录&quot;">​</a></h1><p>Grow Admin 的登录能力由 <code>apps-login</code> 提供页面与接口调用，<strong>路由守卫与动态路由注册由 <code>apps-home</code> 负责</strong>。二者分工明确，不要混淆。</p><h2 id="模块职责" tabindex="-1">模块职责 <a class="header-anchor" href="#模块职责" aria-label="Permalink to &quot;模块职责&quot;">​</a></h2><table><thead><tr><th>模块</th><th>职责</th></tr></thead><tbody><tr><td><code>apps-login</code></td><td>登录页 UI、多形态表单（账号/手机/扫码等）、登录 API 调用、Token 写入</td></tr><tr><td><code>apps-home</code></td><td>路由守卫（Token 校验、白名单、动态路由触发）、Home 布局壳</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">注意</p><p><code>apps-login/src/routes/guard.ts</code> 当前为<strong>占位实现</strong>（直接 <code>next()</code>），真正的认证守卫在 <code>apps-home/src/routes/guard.ts</code>，由 <code>apps-home/library.ts</code> 的 <code>onSetup</code> 注册。</p></div><h2 id="登录流程" tabindex="-1">登录流程 <a class="header-anchor" href="#登录流程" aria-label="Permalink to &quot;登录流程&quot;">​</a></h2><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">用户访问 /</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    ↓</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">Login 页（whiteRoute: true）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    ↓ POST /api/login（Mock 或真实接口）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">useLoginSuccess → sessionStorage[AUTHORITY_TOKEN] = accessToken</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    ↓</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">router.push({ name: &#39;Home&#39; })</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">    ↓</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">apps-home 守卫检测 Token → registerDynamicRoutes() → /home/*</span></span></code></pre></div><h3 id="token-存储" tabindex="-1">Token 存储 <a class="header-anchor" href="#token-存储" aria-label="Permalink to &quot;Token 存储&quot;">​</a></h3><p>登录成功后，<code>useLoginSuccess</code> 将 <code>accessToken</code> 写入 <code>sessionStorage</code>，key 为 <code>AUTHORITY_TOKEN</code>（来自 <code>@grow-admin-rock/constants</code>）。</p><p>守卫通过同一 key 判断登录状态：</p><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#C792EA" })}">function</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">getToken</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">()</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}"> </span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#F07178" })}">  </span><span style="${ssrRenderStyle({ "color": "#89DDFF", "font-style": "italic" })}">return</span><span style="${ssrRenderStyle({ "color": "#F07178" })}"> </span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">sessionStorage</span><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">.</span><span style="${ssrRenderStyle({ "color": "#82AAFF" })}">getItem</span><span style="${ssrRenderStyle({ "color": "#F07178" })}">(</span><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">AUTHORITY_TOKEN</span><span style="${ssrRenderStyle({ "color": "#F07178" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#89DDFF" })}">}</span></span></code></pre></div><h3 id="开发环境测试账号" tabindex="-1">开发环境测试账号 <a class="header-anchor" href="#开发环境测试账号" aria-label="Permalink to &quot;开发环境测试账号&quot;">​</a></h3><p>Mock 接口 <code>sample/mock/auth.ts</code> 提供：</p><table><thead><tr><th>字段</th><th>值</th></tr></thead><tbody><tr><td>用户名</td><td><code>admin</code></td></tr><tr><td>密码</td><td><code>123456</code></td></tr><tr><td>Token</td><td><code>grow-admin-fake-token</code></td></tr></tbody></table><h2 id="路由守卫逻辑-apps-home" tabindex="-1">路由守卫逻辑（apps-home） <a class="header-anchor" href="#路由守卫逻辑-apps-home" aria-label="Permalink to &quot;路由守卫逻辑（apps-home）&quot;">​</a></h2><p><code>createAuthGuard()</code> 在 <code>beforeEach</code> 中处理：</p><table><thead><tr><th>场景</th><th>行为</th></tr></thead><tbody><tr><td>访问白名单路由（<code>meta.whiteRoute</code>）且已有 Token</td><td>重定向到 <code>Home</code></td></tr><tr><td>访问白名单路由且无 Token</td><td>放行</td></tr><tr><td>访问受保护路由且无 Token</td><td>跳转 <code>Login</code>，携带 <code>redirect</code> query</td></tr><tr><td>已登录且动态路由未注册</td><td><code>registerDynamicRoutes()</code> → <code>replace</code> 重试当前路径</td></tr><tr><td>其他</td><td><code>next()</code></td></tr></tbody></table><h2 id="页面级-bootstrap-与守卫的关系" tabindex="-1">页面级 Bootstrap 与守卫的关系 <a class="header-anchor" href="#页面级-bootstrap-与守卫的关系" aria-label="Permalink to &quot;页面级 Bootstrap 与守卫的关系&quot;">​</a></h2><p><code>apps-home</code> 的 <code>useAppBootstrap</code> 在 <code>onMounted</code> 中也会调用 <code>registerDynamicRoutes()</code>，并显示 <code>PageLoading</code>。</p><table><thead><tr><th>触发点</th><th>时机</th><th>作用</th></tr></thead><tbody><tr><td>路由守卫</td><td>首次进入受保护路由（导航前）</td><td>保证直接访问 <code>/home/workspace</code> 时路由已注册</td></tr><tr><td><code>useAppBootstrap</code></td><td>Home 组件挂载后</td><td>拉取菜单、显示页面加载动画</td></tr></tbody></table><p>两者通过 <code>authStore.getIsDynamicAddedRoute</code> 防重复注册。守卫负责<strong>导航正确性</strong>，bootstrap 负责<strong>页面加载体验</strong>。</p><h2 id="登录页组件" tabindex="-1">登录页组件 <a class="header-anchor" href="#登录页组件" aria-label="Permalink to &quot;登录页组件&quot;">​</a></h2><p><code>apps-login</code> 提供多种登录形态：</p><table><thead><tr><th>组件</th><th>说明</th></tr></thead><tbody><tr><td><code>LoginForm</code></td><td>账号密码登录（默认）</td></tr><tr><td><code>ForgetPassword</code></td><td>忘记密码</td></tr><tr><td><code>MobilePhone</code></td><td>手机号 + 验证码</td></tr><tr><td><code>QrCodeLogin</code></td><td>扫码登录</td></tr><tr><td><code>ThirdParty</code></td><td>第三方登录入口</td></tr><tr><td><code>LoginThemeSwitch</code></td><td>暗色模式切换</td></tr><tr><td><code>LoginLanguageSwitch</code></td><td>语言切换</td></tr></tbody></table><p>通过 <code>useLoginEvent</code> 切换 <code>formType</code> 控制展示形态。</p><h2 id="oauth-认证模式-可选" tabindex="-1">OAuth / 认证模式（可选） <a class="header-anchor" href="#oauth-认证模式-可选" aria-label="Permalink to &quot;OAuth / 认证模式（可选）&quot;">​</a></h2><p><code>apps-login/src/usage.ts</code> 提供 <code>useAuthMode()</code>、<code>useOAuth2Config()</code>，读取 <code>useGlobConfig()</code> 中的 <code>authMode</code>、<code>oauthCodeRoute</code> 等。</p><div class="info custom-block"><p class="custom-block-title">前置依赖</p><p><code>useGlobConfig</code> 来自 <code>@grow-admin-rock/hooks</code>，依赖 <code>@grow-admin-rock/settings</code> 装配。当前 <code>sample</code> 宿主<strong>未装配</strong> <code>settings</code> + <code>hooks</code>，OAuth 相关能力需在宿主 <code>initIoc.ts</code> 中额外 <code>.use(settingsLib)</code> / <code>.use(hooksLib)</code> 并配置环境变量后才会生效。</p></div><p>相关环境变量（<code>VITE_GLOB_APP_AUTH_MODE</code> 等）见 <a href="/guide/development/project-setting#环境变量">项目配置 - 环境变量</a>。</p><h2 id="相关-mock-接口" tabindex="-1">相关 Mock 接口 <a class="header-anchor" href="#相关-mock-接口" aria-label="Permalink to &quot;相关 Mock 接口&quot;">​</a></h2><table><thead><tr><th>接口</th><th>文件</th><th>说明</th></tr></thead><tbody><tr><td><code>POST /api/login</code></td><td><code>sample/mock/auth.ts</code></td><td>账号密码登录</td></tr><tr><td><code>POST /api/verification/code</code></td><td><code>sample/mock/login.ts</code></td><td>发送验证码</td></tr><tr><td><code>POST /api/modify/phone/login</code></td><td><code>sample/mock/login.ts</code></td><td>手机号登录</td></tr><tr><td><code>GET /api/menu/list</code></td><td><code>sample/mock/routers.ts</code></td><td>登录后菜单数据</td></tr></tbody></table><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><ul><li><a href="/guide/development/permission-mode">权限模式</a> — 动态菜单来源与角色过滤</li><li><a href="/guide/architecture/routing-and-menu">路由与菜单</a> — 动态路由注册</li><li><a href="/guide/development/mock">Mock 数据</a> — 开发环境接口模拟</li><li><a href="/guide/development/http-infrastructure">HTTP 基础设施</a> — 请求封装与 Transform</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/development/authentication.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const authentication = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  authentication as default
};
