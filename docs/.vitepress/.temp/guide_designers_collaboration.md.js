import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.cc2b3d55.js";
const __pageData = JSON.parse('{"title":"低代码设计器 · 协同工作","description":"","frontmatter":{"title":"低代码设计器 · 协同工作","lang":"zh-CN"},"headers":[],"relativePath":"guide/designers/collaboration.md"}');
const _sfc_main = { name: "guide/designers/collaboration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="协同工作" tabindex="-1">协同工作 <a class="header-anchor" href="#协同工作" aria-label="Permalink to &quot;协同工作&quot;">​</a></h1><p>低代码各模块<strong>不是</strong>绑死成一个单体编辑器，而是靠 <strong>约定好的数据产物 + 运行时 <code>state</code> 总线 + 共享代码能力</strong> 协作。设计时各自产出 JSON；运行时由页面 / 报表统一拉数、绑定、渲染。</p><h2 id="_1-分工-谁负责什么" tabindex="-1">1. 分工：谁负责什么 <a class="header-anchor" href="#_1-分工-谁负责什么" aria-label="Permalink to &quot;1. 分工：谁负责什么&quot;">​</a></h2><table><thead><tr><th>环节</th><th>模块</th><th>输入</th><th>输出</th><th>不做什么</th></tr></thead><tbody><tr><td>物理模型</td><td>数据库建模</td><td>人工建表</td><td><code>DatabaseSchema</code></td><td>不算业务指标、不画页面</td></tr><tr><td>分析模型</td><td>数据准备</td><td>已发布建模元数据</td><td><code>DataPrepDataset</code> + 查询结果表</td><td>不直接绑图表 / 组件</td></tr><tr><td>ETL 管道</td><td>数据清洗</td><td>建模表 / Dataset 表或输出</td><td><code>CleanFlow</code>（声明式，调用时执行）</td><td>设计期可本地预览；不直绑图表 / 不写 <code>state</code></td></tr><tr><td>业务流转</td><td>流程引擎</td><td>人工规则 / 表单 key（规划）</td><td><code>ProcessFlow</code>（声明式，设计期）</td><td>无运行时引擎；不替代数据清洗</td></tr><tr><td>页面展示</td><td>页面设计器</td><td>物料 + <code>state</code></td><td><code>DesignerSchema</code></td><td>不维护表结构</td></tr><tr><td>报表展示</td><td>报表设计器</td><td>网格 + 页面级数据</td><td><code>ReportSchema</code></td><td><strong>不直选</strong> Dataset</td></tr><tr><td>脚本能力</td><td>代码沙箱</td><td>表达式 / SFC</td><td>编辑与预览能力</td><td>不单独存业务 schema</td></tr></tbody></table><p>一句话：<strong>建模管「有什么表」，数据准备管「怎么算」，数据清洗管「怎么洗」，流程引擎管「怎么批」，页面 / 报表管「怎么看」，沙箱管「怎么写逻辑」。</strong></p><h2 id="_2-衔接点-靠什么串起来" tabindex="-1">2. 衔接点：靠什么串起来 <a class="header-anchor" href="#_2-衔接点-靠什么串起来" aria-label="Permalink to &quot;2. 衔接点：靠什么串起来&quot;">​</a></h2><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">┌─────────────────┐     发布元数据      ┌─────────────────┐</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ 数据库建模       │ ───────────────► │ 数据准备         │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ DatabaseSchema  │   (schemas API)   │ DataPrepDataset │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">└────────┬────────┘                   └────────┬────────┘</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">         │                                     │ POST …/query</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">         │          ┌──────────────────────────┤ columns + rows</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">         │          │                          ▼</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">         │          │               ┌─────────────────────┐</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">         ▼          ▼               │ 页面 / 报表运行时    │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">┌─────────────────┐                 │ buildRuntimeState   │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ 数据清洗         │  调用时执行     │   → state.xxx       │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ CleanFlow       │ ─(规划中)─────► └──────────┬──────────┘</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">│ (建模表/Dataset)│   report/page/api          │</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">└─────────────────┘                 ┌──────────┼──────────┐</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                                    ▼          ▼          ▼</span></span>
<span class="line"><span style="${ssrRenderStyle({ "color": "#A6ACCD" })}">                             组件 props    图表绑定    事件/计算</span></span></code></pre></div><table><thead><tr><th>衔接</th><th>机制</th><th>说明</th></tr></thead><tbody><tr><td>建模 → 数据准备</td><td>元数据接口</td><td>数据准备「添加表」拉取已发布 schema（演示：<code>GET …/data-prep/schemas</code>、<code>schema-bundle</code>）</td></tr><tr><td>建模 / 数据准备 → 数据清洗</td><td>源节点引用</td><td>「数据表」可选 <code>schema-table</code> / <code>dataset-table</code> / <code>dataset-output</code>（当前为 demo / Mock 下拉）</td></tr><tr><td>数据准备 → 展示</td><td><strong>查询结果进 <code>state</code></strong></td><td>页面 / 报表用「数据请求」调 <code>POST …/data-prep/query</code>（或本地 <code>queryDatasetLocal</code>），在 <code>fit</code> / <code>didFetch</code> 里写入 <code>state</code>；也可用「属性计算」整理成类目 / 系列数组</td></tr><tr><td>数据清洗 → 展示</td><td><strong>调用时执行（规划）</strong></td><td>输出节点 <code>target</code>: report / lowcode / api；消费者绑定后续对接。当前演示主路径仍是数据准备 → <code>state</code></td></tr><tr><td>流程引擎 → 页面</td><td><strong>formKey / 运行时（规划）</strong></td><td>人工节点可配 <code>formKey</code>；执行引擎与待办通道未接通。当前仅保存 <code>ProcessFlow</code> 定义</td></tr><tr><td>页面 ↔ 报表</td><td><strong>同一套页面级数据模型</strong></td><td>二者共用 <code>dataSource</code> / <code>apiOutlined</code> / <code>computedProps</code> 与 <code>buildRuntimeState</code>；报表区块再用 <code>dataBinding</code> 从 <code>state</code> 取数</td></tr><tr><td>各设计器 → 沙箱</td><td><strong>内嵌编辑器</strong></td><td>事件脚本、变量 / 函数绑定、公式编辑、SQL 文本等，底层是 <code>GrowCodeEditor</code> 等，不各自造轮子</td></tr></tbody></table><h2 id="_3-运行时约定-统一的-state" tabindex="-1">3. 运行时约定：统一的 <code>state</code> <a class="header-anchor" href="#_3-运行时约定-统一的-state" aria-label="Permalink to &quot;3. 运行时约定：统一的 \`state\`&quot;">​</a></h2><p>页面设计器与报表设计器在运行时（预览 / <code>GrowRenderer</code> / <code>GrowReportRenderer</code>）走同一套路：</p><ol><li><code>dataSource</code> + <code>computedProps</code> → <code>buildRuntimeState</code> → 得到 <code>state</code></li><li><code>apiOutlined</code> 按生命周期拉数，结果写回 <code>state</code>（或经处理器改写）</li><li>展示侧只认 <code>state.xxx</code>： <ul><li>页面：组件属性 <code>propBindModes: &#39;bind&#39;</code> → 如 <code>state.title</code></li><li>报表：区块 <code>dataBinding.xAxisData</code> / <code>seriesData[i]</code> → 如 <code>state.regions</code>、<code>state.amounts</code></li></ul></li></ol><p>因此 <strong>数据准备与图表之间没有专用「Dataset 选择器」</strong>：中间必须经过「查询 → 写入 state → 绑定」这一步。好处是页面和报表操作习惯一致，也方便用同一接口服务多种展示。</p><p>可选适配：包内 <code>toCartesianSeriesPayload(result, categoryField, seriesFields)</code> 可把查询结果整理成笛卡尔轴 / 系列，再赋给 <code>state</code>，减轻绑定负担。</p><h2 id="_4-设计时复用-报表站在页面肩膀上" tabindex="-1">4. 设计时复用：报表站在页面肩膀上 <a class="header-anchor" href="#_4-设计时复用-报表站在页面肩膀上" aria-label="Permalink to &quot;4. 设计时复用：报表站在页面肩膀上&quot;">​</a></h2><p>报表设计器<strong>直接复用</strong> <code>@grow-admin-rock/designer</code> 的页面级面板与运行时（数据源、计算属性、数据请求、页面事件等），只在画布与右侧换成「网格区块 + 图表配置 + 数据绑定」。</p><p>这意味着：</p><ul><li>同一份「怎么拿数」的配置思路，页面和报表通用</li><li>报表多出来的只有：<code>layout</code>（网格）与 <code>chartConfig</code> / <code>dataBinding</code>（怎么画）</li></ul><h2 id="_5-端到端示例-演示环境可照做" tabindex="-1">5. 端到端示例（演示环境可照做） <a class="header-anchor" href="#_5-端到端示例-演示环境可照做" aria-label="Permalink to &quot;5. 端到端示例（演示环境可照做）&quot;">​</a></h2><p>目标：一张「按区域汇总订单金额」的柱状图。</p><ol><li><strong>数据库建模</strong>（可选，演示已有 Mock 建模）<br> 建好 <code>orders</code> 等表，或直接用 Mock 的 <code>schema_demo_sales</code>。</li><li><strong>数据准备</strong><ul><li>添加 <code>orders</code> 表</li><li>配置度量：维度 <code>orders.region</code>，公式 <code>SUM([orders.amount])</code>，<code>outputKey: amount</code></li><li>在「数据输出」勾选维度与 <code>amount</code> → 预览 → 保存 Dataset</li></ul></li><li><strong>报表设计器</strong><ul><li>左轨「数据请求」：请求 <code>…/data-prep/query</code>，body 带 <code>datasetId</code>（或完整 dataset）</li><li>在 <code>didFetch</code> / <code>fit</code> 中把 <code>rows</code> 转成类目数组与数值数组，写入例如 <code>state.regionNames</code>、<code>state.regionAmounts</code>（或先整表写入再「属性计算」拆列）</li><li>添加柱状图区块：<code>xAxisData</code> → <code>state.regionNames</code>，<code>seriesData[0]</code> → <code>state.regionAmounts</code></li></ul></li><li><strong>预览</strong><br> 运行时拉数 → 更新 <code>state</code> → 绑定注入 ECharts。</li></ol><p>页面设计器同理：用表格 / 统计卡片绑定同一批 <code>state</code> 字段即可，不必再配一套 Dataset。</p><p>可选：<strong>数据清洗</strong> playground 中编排源表 → 过滤 / 拆分 → 输出流，用本地管道预览结果并保存 <code>CleanFlow</code> 快照；与图表的正式对接待生产「调用时执行」与消费者绑定落地后启用。详见 <a href="/data-clean/">数据清洗</a>。</p><h2 id="_6-协作边界-避免误解" tabindex="-1">6. 协作边界（避免误解） <a class="header-anchor" href="#_6-协作边界-避免误解" aria-label="Permalink to &quot;6. 协作边界（避免误解）&quot;">​</a></h2><table><thead><tr><th>容易误解</th><th>实际约定</th></tr></thead><tbody><tr><td>报表里直接选 Dataset</td><td>否；经 <code>state</code> 间接消费</td></tr><tr><td>数据准备自动继承建模的外键当 Join</td><td>否；<code>joins</code> 需在数据准备里手动配</td></tr><tr><td>建模里的 SQL 查询会自动被数据准备执行</td><td>否；<code>queries</code> 本版为建模侧存档；分析查询走 Dataset</td></tr><tr><td>数据清洗预览 = 生产调用结果</td><td>否；当前为 <code>runCleanFlowLocal</code> + Demo/Mock 表的设计期管道，未写入页面/报表 <code>state</code></td></tr><tr><td>数据清洗已替代数据准备</td><td>否；二者互补（分析查询 vs ETL 流）</td></tr><tr><td>流程引擎 = 数据清洗</td><td>否；流程管业务审批/编排，清洗管数据管道</td></tr><tr><td>流程引擎已可跑实例</td><td>否；当前仅设计期编排，无运行时引擎</td></tr><tr><td>各设计器共用一份总 schema</td><td>否；各自产物独立持久化，靠接口与 <code>state</code> 协作</td></tr><tr><td>代码沙箱是「画布设计器」</td><td>否；是共享编辑 / 预览基础设施</td></tr></tbody></table><h2 id="_7-推荐使用顺序" tabindex="-1">7. 推荐使用顺序 <a class="header-anchor" href="#_7-推荐使用顺序" aria-label="Permalink to &quot;7. 推荐使用顺序&quot;">​</a></h2><ol><li><strong>建模</strong>：在「设计器 → 数据库建模」建表、字段与关联，导出 / 保存 schema</li><li><strong>数据准备</strong>：在「设计器 → 数据准备」选表、连线 Join、配置维度 / 公式度量与输出字段，预览并保存 Dataset</li><li><strong>数据清洗</strong>（可选）：在「设计器 → 数据清洗」拖拽编排清洗流，配置过滤 / 分支 / 拆分等，保存 <code>CleanFlow</code></li><li><strong>流程引擎</strong>（可选）：在「设计器 → 流程引擎」编排审批 / 事件 / 系统节点，保存 <code>ProcessFlow</code></li><li><strong>展示</strong>： <ul><li>页面：配置数据源 / 数据请求，把查询结果写入 <code>state</code>，组件属性绑定 <code>state.xxx</code></li><li>报表：同样配置页面级数据，再在区块「数据绑定」中把 <code>state.xxx</code> 注入轴 / 系列</li></ul></li><li><strong>脚本</strong>：事件 / 计算属性 / 函数绑定需要时再写（基于代码沙箱）</li></ol><p>深入阅读：<a href="/data-prep/usage#与页面--报表对接">数据准备 · 对接</a>、<a href="/data-clean/usage#与页面--报表对接规划">数据清洗 · 对接</a>、<a href="/process-engine/">流程引擎</a>、<a href="/report-designer/data-binding">报表 · 数据绑定</a>、<a href="/page-designer/variable-bind">页面 · 变量绑定</a>、<a href="/page-designer/events">页面 · 事件</a>。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/designers/collaboration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const collaboration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  collaboration as default
};
