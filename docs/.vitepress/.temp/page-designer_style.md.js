import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.cc2b3d55.js";
const __pageData = JSON.parse('{"title":"页面设计器 · 样式面板","description":"","frontmatter":{"title":"页面设计器 · 样式面板","lang":"zh-CN"},"headers":[],"relativePath":"page-designer/style.md"}');
const _sfc_main = { name: "page-designer/style.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="样式面板" tabindex="-1">样式面板 <a class="header-anchor" href="#样式面板" aria-label="Permalink to &quot;样式面板&quot;">​</a></h1><p>右侧「样式」Tab 将常用 CSS 写成可视化配置，写入当前节点的 <code>styles[uuid]</code>，并作用在<strong>映射组件</strong>上（而非仅选区外框）。</p><h2 id="样式分组" tabindex="-1">样式分组 <a class="header-anchor" href="#样式分组" aria-label="Permalink to &quot;样式分组&quot;">​</a></h2><table><thead><tr><th>分组</th><th>说明</th><th>可见范围（概要）</th></tr></thead><tbody><tr><td><strong>尺寸与间距</strong></td><td>宽高、Margin、Padding，支持 px / % / vw·vh</td><td>全部</td></tr><tr><td><strong>圆角</strong></td><td>四角半径；可四角联动</td><td>全部</td></tr><tr><td><strong>边框</strong></td><td>颜色 / 透明度、线型、宽度；可四边联动</td><td>图片、标题、正文、短语、容器</td></tr><tr><td><strong>背景颜色</strong></td><td>色板 + 十六进制 + 透明度</td><td>标题、正文、短语、容器</td></tr><tr><td><strong>阴影</strong></td><td>阴影色 + 透明度（写入 <code>box-shadow</code>）</td><td>图片、标题、正文、短语、容器</td></tr><tr><td><strong>布局模式</strong></td><td><code>display</code> 与 flex 子项（主轴 / 对齐 / 换行）</td><td>标题、正文、短语、容器、链接</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>具体白名单以 <code>styleConfig.vue</code> 中 <code>*_ALLOWED_TAGS</code> 为准；未列出的组件不显示对应分组。</p></div><h2 id="布局模式-display" tabindex="-1">布局模式（display） <a class="header-anchor" href="#布局模式-display" aria-label="Permalink to &quot;布局模式（display）&quot;">​</a></h2><p>可选：<code>inline</code> / <code>flex</code> / <code>block</code> / <code>inline-block</code>。</p><p>当 <code>display === &#39;flex&#39;</code> 时，可继续配置：</p><ul><li><strong>主轴方向</strong> <code>flex-direction</code></li><li><strong>主轴对齐</strong> <code>justify-content</code></li><li><strong>辅轴对齐</strong> <code>align-items</code></li><li><strong>换行</strong> <code>flex-wrap</code></li></ul><p>切换为非 flex 时，会清理上述 flex 相关字段。</p><h3 id="内外层-display-约定" tabindex="-1">内外层 display 约定 <a class="header-anchor" href="#内外层-display-约定" aria-label="Permalink to &quot;内外层 display 约定&quot;">​</a></h3><p>对 <strong>标题、正文、短语、容器、链接</strong>：</p><table><thead><tr><th>层级</th><th><code>display: inline</code> 时的表现</th></tr></thead><tbody><tr><td><strong>选区外框</strong>（<code>draggable-item</code>）</td><td>使用 <code>inline-block</code> + <code>width: fit-content</code>，保证可选中、描边，且不占满整行</td></tr><tr><td><strong>映射组件</strong>（真实节点）</td><td>保持配置中的 <code>inline</code>（或你设置的 <code>inline-block</code> / <code>inline-flex</code>）</td></tr></tbody></table><p>与 <strong>链接</strong> 默认行为对齐：行内级不携带默认 <code>width: 100%</code>。从块级切到行内时，样式面板会去掉历史遗留的 <code>width: 100%</code>；切回 <code>block</code> / <code>flex</code> 时，标题 / 正文 / 容器会恢复默认占满宽度。</p><h3 id="flex-与画布" tabindex="-1">Flex 与画布 <a class="header-anchor" href="#flex-与画布" aria-label="Permalink to &quot;Flex 与画布&quot;">​</a></h3><p>容器（<code>div</code>）设为 <code>display: flex</code> 时，样式作用在<strong>子节点列表容器</strong>（拖拽列表根节点）上，子选区才会按主轴横向 / 纵向排列。仅设在更外层空壳上不会生效。</p><h2 id="尺寸与选区外框" tabindex="-1">尺寸与选区外框 <a class="header-anchor" href="#尺寸与选区外框" aria-label="Permalink to &quot;尺寸与选区外框&quot;">​</a></h2><ul><li>宽高写在 <code>styles</code> 中；选区外框会同步尺寸相关属性，使选中框与内容一致</li><li>映射组件在「外框已承接尺寸」时，内部可用 <code>width/height: 100%</code> 铺满（<strong>行内级除外</strong>，避免再次撑满整行）</li></ul><h2 id="文字样式" tabindex="-1">文字样式 <a class="header-anchor" href="#文字样式" aria-label="Permalink to &quot;文字样式&quot;">​</a></h2><p>标题 / 正文 / 短语在样式面板中还有 <strong>文字</strong> 分组（<code>ElementText</code>）：颜色、字号、粗体 / 斜体 / 下划线 / 删除线、对齐、字距、行高等。</p><p>源码：<code>GrowDesigner/optionComponent/ElementText/</code>。</p><h2 id="实现位置-便于对照源码" tabindex="-1">实现位置（便于对照源码） <a class="header-anchor" href="#实现位置-便于对照源码" aria-label="Permalink to &quot;实现位置（便于对照源码）&quot;">​</a></h2><table><thead><tr><th>能力</th><th>路径</th></tr></thead><tbody><tr><td>样式分组入口</td><td><code>GrowDesigner/components/eleOptions/styleConfig.vue</code></td></tr><tr><td>尺寸 / 圆角 / 边框 / 背景 / 阴影 / 布局</td><td><code>GrowDesigner/optionComponent/Element*</code></td></tr><tr><td>文字</td><td><code>GrowDesigner/optionComponent/ElementText</code></td></tr><tr><td>选区外框与 display</td><td><code>GrowDesigner/components/draggableItem</code></td></tr><tr><td>样式应用到映射节点</td><td><code>GrowDesigner/components/abstractionComponent</code></td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("page-designer/style.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const style = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  style as default
};
